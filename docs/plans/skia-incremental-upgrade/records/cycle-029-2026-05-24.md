# Skia Incremental Upgrade Cycle 029

作業日時: 2026-05-24 10:55:21 JST

## Summary

- baseline: `c73cff97952aa15e01985a35e5c6575b4eb50454` (`2024-07-10`)
- accepted candidate: `84d893159af295c4dc61f408f227cf37916b5b55`
- candidate date: `2024-07-18T23:57:14Z`
- subject: `Disable infinity support for ANGLE + Metal + Intel`
- result: accepted

## Candidate Selection

cycle 028 の next note と handoff に従い、deferred していた `84d893159af295c4dc61f408f227cf37916b5b55` 周辺を再比較した。2-week/3-week 候補は Graphite/SkSL/source drift が広がるため、1-week 幅の `84d893...` を採用した。

| candidate | result | note |
| --- | --- | --- |
| `84d893159af295c4dc61f408f227cf37916b5b55` | accepted | 116 commits from baseline; `include`/`modules` drift 11 files, 181 insertions, 218 deletions |
| `746d444f3efdc41216d94ae53b07bac3c949f887` | deferred | 215 commits from baseline; `include`/`modules` drift 19 files, 237 insertions, 274 deletions |
| `0a7c7b0b96fc897040e71ea3304d9d6a042cda8b` | deferred | 324 commits from baseline; `include`/`modules` drift 27 files, 326 insertions, 617 deletions |

accepted candidate の `DEPS` / `gn` / `bazel` / `include` / `modules` / `src` の source/dependency drift は `114 files changed, 5084 insertions(+), 2507 deletions(-)`。

主要 public/private header:

- `include/gpu/graphite/BackendSemaphore.h`
- `include/gpu/graphite/BackendTexture.h`
- `include/gpu/graphite/TextureInfo.h`
- `include/gpu/graphite/mtl/MtlGraphiteTypes.h`
- `include/gpu/graphite/precompile/PrecompileRuntimeEffect.h`
- `include/private/gpu/graphite/MtlGraphiteTypesPriv.h` -> `src/gpu/graphite/mtl/MtlGraphiteTypesPriv.h`
- `src/gpu/graphite/BackendSemaphorePriv.h`
- `src/gpu/graphite/BackendTexturePriv.h`
- `src/gpu/graphite/TextureInfoPriv.h`

## Coverage And Routing

Initial candidate coverage:

- `covered 2812`
- `false_positive 288`
- `na 225`
- `no_public_methods_found 106`
- `split_covered 33`
- `missing 0`
- `deferred 0`
- `partial 0`
- `overcovered 0`

Initial stale C API report:

- `stale_capi 4`
- `signature_changed_review 24`

Final coverage:

- `covered 2812`
- `false_positive 288`
- `na 225`
- `no_public_methods_found 106`
- `split_covered 33`
- `missing 0`
- `deferred 0`
- `partial 0`
- `overcovered 0`

Final stale C API report:

- `stale_capi 4`
- `signature_changed_review 24`

Stale C API retained with replacement public API:

- `Graphite_BackendSemaphore_getMtlEvent`
- `Graphite_BackendSemaphore_getMtlValue`
- `Graphite_BackendTexture_getMtlTexture`
- `Graphite_TextureInfo_getMtlTextureInfo`

These entries are not removed in cycle 029. Upstream moved the Metal public accessors from class methods to namespace helpers in `include/gpu/graphite/mtl/MtlGraphiteTypes.h`. Reskia keeps the C ABI names for compatibility and now implements them through `BackendSemaphores::GetMtlEvent`, `BackendSemaphores::GetMtlValue`, `BackendTextures::GetMtlTexture`, and `TextureInfos::GetMtlTextureInfo`. Metal constructors were similarly moved to `MakeMetal` helper calls. Deletion is not planned for this cycle because the upstream functionality remains public under replacement helpers.

Routing:

- `gpu-ganesh-graphite`: Graphite backend private data migration, Metal helper split, `PrecompileRuntimeEffect`, and SkSL specialization/codegen drift were source-synced.
- `core-codec-low-risk`: `SkRRect`, `SkStroke`, `skcms`, and low-risk source updates were source-synced.
- `text-module`: `src/text/gpu/SubRunContainer.cpp` update was source-synced.
- `optional-backend`: Metal/Vulkan/Dawn source drift was synced behind existing feature flags; Dawn/Vulkan remain optional and Metal smoke passed.
- `removed-upstream`: no public C API removal was required. Four Graphite Metal class-method stale entries are retained because public namespace helper replacements exist.

## Source/Header Sync

Synced tracked candidate drift from `DEPS`, public/private `include`, mirrored `modules`, and mirrored `src` trees.

Main sync:

- Graphite `BackendSemaphore`, `BackendTexture`, and `TextureInfo` private data migration.
- Metal Graphite helper split into `MtlBackendSemaphore.mm`, `MtlBackendTexture.mm`, `MtlTextureInfo.mm`, and moved `MtlGraphiteTypesPriv.h`.
- Graphite precompile runtime effect sources.
- Ganesh/Graphite GPU source updates, including Metal, Vulkan, and Dawn backend drift.
- SkSL specialization and generated SkSL updates.
- Core `SkRRect`/`SkStroke`, `skcms`, fontconfig, and text GPU updates.

CMake/source-list:

- Added `src/sksl/analysis/SkSLSpecialization.cpp` to `cmake/reskia/sources-core.cmake`.
- Enabled `OBJCXX` on Apple and compiled `capi/sk_gpu_context.cpp` as Objective-C++ when `RESKIA_ENABLE_GPU_METAL=ON`, because candidate Graphite Metal helpers expose Objective-C types under `__OBJC__`.

Intentionally not synced:

- Root `gn/*.gni`, root `bazel/` metadata, untracked SkSL lexer metadata, and other untracked upstream metadata remain outside Reskia's tracked mirror surface.

## C API Catch-up

Implemented:

- Updated Graphite Metal C API implementations to call `TextureInfos::MakeMetal`, `TextureInfos::GetMtlTextureInfo`, `BackendTextures::MakeMetal`, `BackendTextures::GetMtlTexture`, `BackendSemaphores::MakeMetal`, `BackendSemaphores::GetMtlEvent`, and `BackendSemaphores::GetMtlValue`.
- Added explicit Metal enum casts for `MtlTextureInfo` construction.

Removed because upstream removed:

- No public C API removal was required.

Classified:

- Four stale Graphite Metal accessors are retained with documented upstream namespace-helper replacements.
- `signature_changed_review` rows are ABI-compatible routing noise from upstream signature/helper drift; no C ABI signature change was required.

## Verification

| gate | result | command/log |
| --- | --- | --- |
| initial coverage regression | pass | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output .tmp/skia-upgrade-cycle-029/candidate-public-api-coverage-matrix.csv --previous-matrix docs/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --stale-output .tmp/skia-upgrade-cycle-029/candidate-stale-capi.csv` |
| canonical coverage regeneration | pass | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output docs/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --previous-matrix .tmp/skia-upgrade-cycle-029/previous-public-api-coverage-matrix.csv --stale-output docs/plans/c-binding-remediation/checklists/public-api-stale-capi.csv` |
| stale C API report | accepted with documented retention | canonical stale report has `stale_capi 4` and `signature_changed_review 24`; four stale Graphite Metal C API names are retained through upstream public helper replacements |
| prebuilt configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle029-prebuilt -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle029-prebuilt -j 8` |
| source configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle029-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle029-source -j 8` |
| GPU smoke build/ctest | pass | `cmake -S skia -B skia/cmake-build-codex-cycle029-gpu-smoke -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_GPU_GANESH=ON -DRESKIA_ENABLE_GPU_GRAPHITE=ON -DRESKIA_ENABLE_GPU_METAL=ON`; `cmake --build ...`; `ctest --test-dir skia/cmake-build-codex-cycle029-gpu-smoke -R 'c_skia_gpu_(context|surface)_capi_smoke' --output-on-failure` -> 2/2 pass |
| source SVG/provider/text smoke build/ctest | pass | `cmake -S skia -B skia/cmake-build-codex-cycle029-source-smoke -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_SKPARAGRAPH=ON`; `cmake --build ...`; `ctest --test-dir skia/cmake-build-codex-cycle029-source-smoke -R 'c_skia_(shaping_smoke|shaper_capi_smoke|svg_dom_capi_smoke|skresources_capi_smoke|unicode_capi_smoke|skparagraph_smoke|paragraph_builder_capi_smoke|path_invalid_input_smoke)' --output-on-failure` -> 8/8 pass |

Notes:

- Existing warnings observed: Metal `fastMathEnabled` deprecation, `stb_image_write.h` `sprintf` deprecation, and prebuilt `libsvg.a` macOS deployment-version linker warning.

## Lock Update

`vendor/skia-source.lock`:

- `SKIA_REF="84d893159af295c4dc61f408f227cf37916b5b55"`
- `SKIA_BASELINE_DATE="2024-07-18"`

`vendor/skia-upstream` と `vendor/skia-upstream-candidate` は accepted candidate `84d893159af295c4dc61f408f227cf37916b5b55` に同期済み。

## Next Cycle Notes

- 次サイクルでは accepted baseline `84d893159af295c4dc61f408f227cf37916b5b55` から再比較する。
- 1-week/2-week 候補として `746d444f3efdc41216d94ae53b07bac3c949f887` / `0a7c7b0b96fc897040e71ea3304d9d6a042cda8b` 周辺を再比較する。
- 既知リスクは Graphite/SkSL drift、Metal/Vulkan optional backend drift、SkSL generated churn、Dawn/Android metadata churn。
