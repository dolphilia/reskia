# Skia Incremental Upgrade Cycle 026

作業日時: 2026-05-24 09:57:53 JST

## Summary

- baseline: `24a4123fc949aad0c98d251b05c8ba2b21a9b931` (`2024-06-18`)
- accepted candidate: `a8c2acc3903806ff36800a67f5e60dba84265fd3`
- candidate date: `2024-06-25T20:40:23Z`
- subject: `Change wacky_yuv GMs to split full colorspaces from limited.`
- result: accepted

## Candidate Selection

cycle 025 の next note に従い、Vulkan public header removal と Graphite `PrecompileColorFilter` 追加を含む 1-week 候補を採用した。

| candidate | result | note |
| --- | --- | --- |
| `a8c2acc3903806ff36800a67f5e60dba84265fd3` | accepted | 65 commits; `include`/`modules` drift 12 files |
| `7c69f39fa85b3cca07c7d433a396011e01c88f34` | deferred | 153 commits; `include`/`modules` drift 17 files; additional Graphite precompile image/mask filter API |
| `c73cff97952aa15e01985a35e5c6575b4eb50454` | deferred | 219 commits; `include`/`modules` drift 115 files, mostly Bazel metadata churn |

accepted candidate の `include` / `modules` diff は `12 files changed, 131 insertions(+), 82 deletions(-)`。`DEPS` / `include` / `modules` / `src` の source/dependency drift は `137 files changed, 2872 insertions(+), 1744 deletions(-)`。

## Coverage And Routing

Initial candidate coverage:

- `covered 2814`
- `false_positive 286`
- `na 224`
- `no_public_methods_found 105`
- `split_covered 33`
- `missing 3`

Initial stale C API report:

- `stale_capi 0`
- `signature_changed_review 0`

Final coverage:

- `covered 2816`
- `false_positive 286`
- `na 225`
- `no_public_methods_found 105`
- `split_covered 33`
- `missing 0`
- `deferred 0`
- `partial 0`
- `overcovered 0`

Final stale C API report:

- `stale_capi 0`
- `signature_changed_review 0`

Routing:

- `core-codec-low-risk`: `SkImage::makeScaled` 2 overloads were implemented in C API.
- `gpu-ganesh-graphite`: Graphite precompile color-filter implementation was mirrored for optional Graphite build surface.
- `design-required`: `PrecompileColorFilter::makeComposed` was classified `na` with the existing Graphite precompile ownership/design policy.
- `removed-upstream`: `include/gpu/vk/GrVkBackendContext.h`, `GrVkExtensions.h`, and `GrVkMemoryAllocator.h` were removed upstream. stale C API report stayed 0, so no C API deletion was required.

## Source/Header Sync

Synced candidate diff from `DEPS`, public `include`, `modules/sksg`, and mirrored `src`.

Main sync:

- Public core/image headers and image implementation for `SkImage::makeScaled`.
- Vulkan public header removal and Vulkan private/source updates.
- Graphite precompile color-filter public header and implementation split.
- Graphite/Ganesh image, shader, pipeline, and Metal/Vulkan source updates.
- Codec swizzler/BMP updates, SkSL codegen/generated updates, fontations/CoreText updates.
- `src/utils/SkGetExecutablePath*` files were mirrored, but not added to Reskia's CMake build surface.

CMake/source-list:

- Existing Graphite precompile glob picked up `PrecompileColorFilter.cpp`.
- `src/gpu/graphite/Precompile.cpp` and `FactoryFunctionsPriv.h` were removed with the upstream split.
- `SkSLSPIRVtoHLSL.cpp` kept Reskia's `SK_ENABLE_SPIRV_CROSS` guard because Reskia's current dependency model does not provide SPIRV-Cross HLSL headers by default.

## C API Catch-up

Implemented:

- `SkImage_makeScaled`
- `SkImage_makeScaledWithRecorder`

Removed because upstream removed:

- No public C API removal was required.

Classified:

- `PrecompileColorFilter::makeComposed` classified `na` in `public-api-phase-34-upgrade-probe-overrides.csv`.

## Verification

| gate | result | command/log |
| --- | --- | --- |
| initial coverage regression | pass after routing | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output .tmp/skia-upgrade-cycle-026/candidate-public-api-coverage-matrix.csv --previous-matrix docs/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --stale-output .tmp/skia-upgrade-cycle-026/candidate-stale-capi.csv` |
| canonical coverage regeneration | pass | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output docs/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --previous-matrix .tmp/skia-upgrade-cycle-026/previous-public-api-coverage-matrix.csv --stale-output docs/plans/c-binding-remediation/checklists/public-api-stale-capi.csv` |
| stale C API report | pass | canonical stale report has 0 rows |
| prebuilt configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle026-prebuilt -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle026-prebuilt -j 8` |
| source configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle026-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle026-source -j 8` |
| GPU smoke build/ctest | pass | `cmake -S skia -B skia/cmake-build-codex-cycle026-gpu-smoke -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_GPU_GANESH=ON -DRESKIA_ENABLE_GPU_GRAPHITE=ON -DRESKIA_ENABLE_GPU_METAL=ON`; `cmake --build ...`; `ctest --test-dir skia/cmake-build-codex-cycle026-gpu-smoke -R 'c_skia_gpu_(context|surface)_capi_smoke' --output-on-failure` -> 2/2 pass |
| source SVG/provider/text smoke build/ctest | pass | `cmake -S skia -B skia/cmake-build-codex-cycle026-source-smoke -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_SKPARAGRAPH=ON`; `cmake --build ...`; `ctest --test-dir skia/cmake-build-codex-cycle026-source-smoke -R 'c_skia_(shaping_smoke|shaper_capi_smoke|svg_dom_capi_smoke|skresources_capi_smoke|unicode_capi_smoke|skparagraph_smoke|paragraph_builder_capi_smoke|path_invalid_input_smoke)' --output-on-failure` -> 8/8 pass |

Notes:

- Existing warnings observed: Metal `fastMathEnabled` deprecation, `stb_image_write.h` `sprintf` deprecation, and prebuilt `libsvg.a` macOS deployment-version linker warning.

## Lock Update

`vendor/skia-source.lock`:

- `SKIA_REF="a8c2acc3903806ff36800a67f5e60dba84265fd3"`
- `SKIA_BASELINE_DATE="2024-06-25"`

`vendor/skia-upstream` と `vendor/skia-upstream-candidate` は accepted candidate `a8c2acc3903806ff36800a67f5e60dba84265fd3` に同期済み。

## Next Cycle Notes

- 次サイクルでは accepted baseline `a8c2acc3903806ff36800a67f5e60dba84265fd3` から再比較する。
- 1-week 相当候補は `7c69f39fa85b3cca07c7d433a396011e01c88f34`。追加で Graphite `PrecompileImageFilter` / `PrecompileMaskFilter` public API を含む。
- 2-week 相当候補は `c73cff97952aa15e01985a35e5c6575b4eb50454` 付近だが、Bazel metadata churn が大きいため 1-week 候補を第一候補にする。
