# Skia Incremental Upgrade Cycle 023

作業日時: 2026-05-24 04:09:00 JST

## Summary

- baseline: `78069713e02aae7f03d20114a8bebe8c6a89259a` (`2024-05-21`)
- accepted candidate: `73e53abdf54f56fe30d60dba6e62e19597fa2618`
- candidate date: `2024-06-04T20:41:40Z`
- subject: `[graphite] Use explicit key for analytic rrect blur masks`
- result: accepted

## Candidate Selection

cycle 022 の next note に従い、accepted baseline から 1-week/2-week/3-week 候補を比較した。

| candidate | result | note |
| --- | --- | --- |
| `0026a60471337507a6557a492bd8f0c95a688e17` | deferred | 1-week candidate; 80 commits, `include`/`modules` drift 11 files |
| `73e53abdf54f56fe30d60dba6e62e19597fa2618` | accepted | 2-week candidate; 170 commits, standard-width probe |
| `51eabd0d1e4466eb427394912eddb6f7a9d0cafb` | deferred | 3-week candidate; 263 commits and broader source drift |

accepted candidate の `include` / `modules` diff は `16 files changed, 141 insertions(+), 179 deletions(-)`。`DEPS` / `gn` / `include` / `modules` / `src` の source/dependency drift は `137 files changed, 3711 insertions(+), 2354 deletions(-)`。

## Coverage And Routing

Initial candidate coverage:

- `covered 2814`
- `false_positive 277`
- `na 204`
- `no_public_methods_found 106`
- `split_covered 33`
- `missing 0`

Initial stale C API report:

- `stale_capi 0`
- `signature_changed_review 2`: `SkHighContrastConfig` constructors

Final coverage:

- `covered 2814`
- `false_positive 277`
- `na 204`
- `no_public_methods_found 106`
- `split_covered 33`
- `missing 0`
- `deferred 0`
- `partial 0`
- `overcovered 0`

Final stale C API report:

- `stale_capi 0`
- `signature_changed_review 0`

Routing:

- `core-codec-low-risk`: `SkCanvas`, `SkExif`, TIFF/JPEG metadata helpers, `SkICC`, `SkStreamPriv`, gradient updates.
- `text-module`: Skottie text and CoreText shaper sync only; no new C API.
- `gpu-ganesh-graphite`: Graphite/Ganesh render/text blur and resource-key updates; CMake glob picked up new source names.
- `optional-backend`: Vulkan YCbCr conversion rename follows existing optional Vulkan/Graphite source mirror.
- `removed-upstream`: `VulkanSamplerYcbcrConversion` cpp/header removed and replaced by `VulkanYcbcrConversion` cpp/header.

`SkHighContrastConfig` の `signature_changed_review` は upstream 側の initializer / indentation only の差分で C ABI は互換。canonical matrix refresh 後に final stale report は 0 になった。

## Source/Header Sync

candidate から `DEPS`、`include`、`modules`、`src` の mirrored diff を同期した。Reskia が追跡していない `gn` や PathKit metadata は CMake build surface へ追加しない。

主な同期:

- Core/canvas/image-filter helpers.
- Effects: `SkHighContrastFilter`, shader/table mask filters.
- Codec/encode: Exif, TIFF, JPEG metadata, Gainmap, ICC writer updates.
- GPU/Ganesh/Graphite/Metal/Vulkan updates including Graphite analytic blur and text render steps.
- Skottie/skshaper text updates.
- SKSL generated Graphite shader updates.

CMake/source-list:

- Graphite/Vulkan related source additions and removals are covered by existing CMake globs.
- No manual CMake source-list edit was needed.

Reskia-local follow-up:

- `skia/src/encode/SkICC.cpp` に、Reskia C API が公開している `SkICCFloatXYZD50ToGrid16Lab` と `SkICCFloatToTable16` の definitions を保持した。upstream mirror sync で落ちると `sk_icc.cpp` の link が未定義になるため。

## C API Catch-up

Implemented:

- No new C API entry points were required.

Removed because upstream removed:

- No public C API removal was required. Internal mirrored files `VulkanSamplerYcbcrConversion.cpp` and `.h` were removed with upstream rename.

Classified:

- No new `na` or `false_positive` override was required.

## Verification

| gate | result | command/log |
| --- | --- | --- |
| coverage regeneration | pass | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output .tmp/skia-upgrade-cycle-023/final-public-api-coverage-matrix.csv --previous-matrix docs/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --stale-output .tmp/skia-upgrade-cycle-023/final-stale-capi.csv` |
| stale C API report | pass | final stale report header only; `stale_capi 0`, `signature_changed_review 0` |
| prebuilt configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle023-prebuilt -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle023-prebuilt -j 8` |
| source configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle023-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle023-source -j 8` |
| GPU smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle023-gpu-smoke -R 'c_skia_gpu_(context|surface)_capi_smoke' --output-on-failure` -> 2/2 pass |
| source SVG/provider smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle023-source-smoke -R 'c_skia_(shaping_smoke|shaper_capi_smoke|svg_dom_capi_smoke|skresources_capi_smoke|unicode_capi_smoke|skparagraph_smoke|paragraph_builder_capi_smoke|path_invalid_input_smoke)' --output-on-failure` -> 8/8 pass |
| Skottie/SKSG optional smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle023-skottie-sksg-smoke -R 'c_skia_(skottie_smoke|sksg_smoke|sksg_capi_smoke)' --output-on-failure` -> 3/3 pass |

Notes:

- prebuilt/GPU link では `libsvg.a` の macOS 15.1 object を macOS 15.0 target に link する既存 warning が出る。
- GPU build では Metal `fastMathEnabled` deprecation warning が出るが、対象 smoke は pass。

## Lock Update

`vendor/skia-source.lock`:

- `SKIA_REF="73e53abdf54f56fe30d60dba6e62e19597fa2618"`
- `SKIA_BASELINE_DATE="2024-06-04"`

`vendor/skia-upstream` と `vendor/skia-upstream-candidate` は accepted candidate `73e53abdf54f56fe30d60dba6e62e19597fa2618` に同期済み。

## Next Cycle Notes

- 次サイクルでは accepted baseline `73e53abdf54f56fe30d60dba6e62e19597fa2618` から 1-week/2-week/3-week 候補を再比較する。
- 1-week 候補 `51eabd0d1e4466eb427394912eddb6f7a9d0cafb` は 93 commits、`include`/`modules` drift 17 files、broader drift 69 files。
- 2-week 候補 `24a4123fc949aad0c98d251b05c8ba2b21a9b931` は 166 commits、`include`/`modules` drift 24 files、broader drift 145 files。
- 3-week 候補 `a8c2acc3903806ff36800a67f5e60dba84265fd3` は 231 commits、`include`/`modules` drift 34 files、broader drift 231 files。
- cycle 023 は 2-week 幅で通ったが、次候補は 2-week の `include`/`modules` insertions が大きいため、1-week/2-week の差分内容を見て選ぶ。
