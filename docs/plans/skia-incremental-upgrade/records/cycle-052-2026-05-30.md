# Skia Incremental Upgrade Cycle Record

作成時刻: 2026-05-30 20:41:04 JST

## Summary

- cycle: 052
- branch: `incremental-upgrade`
- baseline commit: `cb1646ca59dbaa37c07a8697c2cb9a0451853932`
- candidate commit: `e56aa7634d2667c25f360c0c4073e996d54d459b`
- candidate date: `2025-05-18T06:32:01-07:00`
- candidate subject: Update SKP version
- result: `accepted`

## Baseline Check

- `vendor/skia-source.lock`: `SKIA_REF="cb1646ca59dbaa37c07a8697c2cb9a0451853932"`, `SKIA_BASELINE_DATE="2025-05-11"`
- `vendor/skia-upstream` HEAD before probe: `cb1646ca59dbaa37c07a8697c2cb9a0451853932`
- coverage summary before probe: `missing 0` / `deferred 0` / `partial 0` / `overcovered 0`; stale report empty
- last known build/smoke baseline: cycle 051 prebuilt/source builds, GPU smoke, and source SVG/provider/text smoke passed

## Candidate Selection

| item | value |
| --- | --- |
| baseline to candidate commits | 55 |
| `include` / `modules` shortstat | 16 files changed, 347 insertions(+), 48 deletions(-) |
| total source-list/dependency drift | 177 files changed, 5691 insertions(+), 1788 deletions(-) |
| primary affected areas | `SkCPUContext` / `SkCPURecorder` / `SkRecorder`, `SkPath` / `SkPathBuilder`, Ganesh/Graphite recorder/context, Graphite Vulkan precompile |
| comparison candidates | 2-week candidate `91dc88dc70e5c7e8debbe21d4d5566d5e9f121e1`: 115 commits, 32 `include`/`modules` files, 269 total files; 3-week local refs for 2025-06-01/02 had no commit |

The 1-week candidate was selected because it keeps the cycle focused on the SkCPU/SkRecorder and Graphite precompile drift. The 2-week candidate adds broader Vulkan dependency roll, Android, `SkImage`/`SkCanvas`, `SkUnicode`, and `skcapture` churn.

## Public Header Delta

主要 header:

- `include/core/SkCPUContext.h`
- `include/core/SkCPURecorder.h`
- `include/core/SkRecorder.h`
- `include/core/SkPath.h`
- `include/core/SkPathBuilder.h`
- `include/core/SkSurface.h`
- `include/gpu/ganesh/GrRecordingContext.h`
- `include/gpu/graphite/Context.h`
- `include/gpu/graphite/Recorder.h`
- `include/gpu/graphite/vk/precompile/VulkanPrecompileShader.h`

注目 API:

- `SkPath::makeInterpolate`, `makeIsVolatile`, `makeOffset`, `ReadFromMemory`
- `SkPathBuilder::transform`, `isEmpty`, `getLastPt`, `setLastPt`, `countPoints`, `isInverseFillType`
- `skgpu::graphite::Recorder::~Recorder`, `Recorder::type`
- `skcpu::Context`, `skcpu::Recorder`, `SkRecorder`

## Coverage Regression

| status | count | note |
| --- | ---: | --- |
| `missing` | 0 | final candidate and final lock matrix |
| `deferred` | 0 | final candidate and final lock matrix |
| `partial` | 0 | final candidate and final lock matrix |
| `overcovered` | 0 | final candidate and final lock matrix |
| `covered` | 2816 | final candidate and final lock matrix |
| `split_covered` | 42 | final candidate and final lock matrix |
| `false_positive` | 290 | final candidate and final lock matrix |
| `na` | 242 | final candidate and final lock matrix |
| `stale_capi` | 20 | final candidate stale report; retained with rationale below |
| `signature_changed_review` | 1 | final candidate stale report; ABI-compatible const-only review below |

Initial candidate coverage found `missing 22`, `partial 1`, `stale_capi 20`, and `signature_changed_review 1`. The final matrix has no `missing` / `deferred` / `partial` / `overcovered`.

The stale rows are existing `SkPath` edit-method C APIs. Upstream moved the edit methods behind the WIP `SK_HIDE_PATH_EDIT_METHODS` guard, but the macro is not enabled in the vendored header, so the methods remain build-visible. The C ABI is retained for compatibility and because removing wrappers before the guard is active would be premature. The `SkPath::makeScale` signature review is const-only and does not change the wrapper ABI.

## Routing

| area | rows/items | decision | owner note |
| --- | ---: | --- | --- |
| `core-codec-low-risk` | `SkPath` immutable helpers, `SkPathBuilder` accessors/mutators | implement | Added null-safe static-handle and builder C API wrappers |
| `gpu-ganesh-graphite` | `Graphite::Recorder` destructor/type, Ganesh/Graphite source drift | implement/sync | Added release alias/type wrapper; GPU smoke covers configured Metal path |
| `optional-backend` | Graphite Vulkan precompile source/header drift | sync source, no new general C ABI | Vulkan remains optional backend surface |
| `design-required` | `skcpu::Context`, `skcpu::Recorder`, `SkRecorder`, `makeCPURecorder` | classify `na` | New unique_ptr recorder ownership needs a common raster/GPU recorder ABI design |
| `removed-upstream` | `SkPath` edit methods reported stale by generator | retain with reason | Guard is WIP and currently not enabled |

## Source/Header Sync

- synced: tracked candidate changes under `skia/DEPS`, `skia/include`, and `skia/src`
- added: new SkCPU recorder/context sources and Graphite Vulkan precompile sources
- CMake/source-list changes: added `src/core/SkCPUContext.cpp`, `src/core/SkCPURecorder.cpp`, and Graphite Vulkan precompile glob
- intentionally not synced: GN/Bazel metadata
- dependency changes: upstream `DEPS` was mirrored for the Skia snapshot record; Reskia dependency mode logic was unchanged

## C API Catch-up

- implemented: `SkPath_makeInterpolate`, `SkPath_makeIsVolatile`, `SkPath_makeOffset`, `SkPath_ReadFromMemory`
- implemented: `SkPathBuilder_transform`, `SkPathBuilder_isEmpty`, `SkPathBuilder_getLastPt`, `SkPathBuilder_setLastPt`, `SkPathBuilder_countPoints`, `SkPathBuilder_isInverseFillType`
- implemented: `Graphite_Recorder_delete`, `Graphite_Recorder_type`
- removed because upstream removed: none
- retained despite stale report: existing `SkPath` edit-method wrappers, because the upstream hide guard is not active
- classified `na`: SkCPU/SkRecorder ownership and `makeCPURecorder` APIs requiring recorder ABI design
- tests added: `c_skia_path_invalid_input_smoke` and `c_skia_path_builder_invalid_input_smoke` now cover null and valid paths for the new wrappers

## Verification

| gate | result | command/log |
| --- | --- | --- |
| coverage regeneration | PASS | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output docs/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv ...` -> `missing 0` / `deferred 0` / `partial 0` / `overcovered 0` |
| stale C API report | PASS with rationale | final candidate stale report: `stale_capi 20`, `signature_changed_review 1`; rationale recorded above. final lock stale report empty against updated matrix |
| prebuilt configure/build | PASS | `cmake -S skia -B skia/cmake-build-codex-cycle052-prebuilt -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle052-prebuilt -j 8` |
| source configure/build | PASS | `cmake -S skia -B skia/cmake-build-codex-cycle052-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle052-source -j 8` |
| GPU smoke | PASS | `cmake -S skia -B skia/cmake-build-codex-cycle052-gpu-smoke -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_GPU_GANESH=ON -DRESKIA_ENABLE_GPU_GRAPHITE=ON -DRESKIA_ENABLE_GPU_METAL=ON`; build passed; `ctest --test-dir skia/cmake-build-codex-cycle052-gpu-smoke -R 'c_skia_gpu_(context|surface)_capi_smoke' --output-on-failure` -> 2/2 pass |
| source SVG/provider smoke | PASS | `cmake -S skia -B skia/cmake-build-codex-cycle052-source-smoke -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_SKPARAGRAPH=ON`; build passed; `ctest --test-dir skia/cmake-build-codex-cycle052-source-smoke -R 'c_skia_(shaping_smoke|shaper_capi_smoke|svg_dom_capi_smoke|skresources_capi_smoke|unicode_capi_smoke|skparagraph_smoke|paragraph_builder_capi_smoke|path_invalid_input_smoke|path_builder_invalid_input_smoke)' --output-on-failure` -> 9/9 pass |
| final lock coverage | PASS | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream --output .tmp/skia-upgrade-cycle-052/final-lock-public-api-coverage-matrix.csv ...` -> `missing 0` / `deferred 0` / `partial 0` / `overcovered 0`; stale report empty |

Notes:

- prebuilt and GPU builds emitted known non-fatal macOS deployment-target warnings from prebuilt static archives.
- builds emitted existing `SkPathOps::TightBounds` deprecation warning and existing test `sprintf` warning.
- GPU build emitted a non-fatal Metal `fastMathEnabled` deprecation warning.

## Lock Update

- updated: yes
- new `SKIA_REF`: `e56aa7634d2667c25f360c0c4073e996d54d459b`
- new `SKIA_BASELINE_DATE`: `2025-05-18`
- reason if not updated: n/a

## Next Cycle Notes

- next candidate target: compare fixed commits after `e56aa7634d2667c25f360c0c4073e996d54d459b`
- width adjustment: compare 1/2/3-week candidates again; cycle 052 deferred `91dc88dc70e5c7e8debbe21d4d5566d5e9f121e1` due to broader churn
- known risks: Vulkan dependency roll, Android and `SkImage`/`SkCanvas` drift, `skcapture` move/removal, `SkUnicode` drift, generated SkSL ES2 deletion, Graphite precompile ABI design, Dawn/WebGPU optional backend C ABI design
