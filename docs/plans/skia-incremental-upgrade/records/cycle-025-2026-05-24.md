# Skia Incremental Upgrade Cycle 025

作業日時: 2026-05-24 09:50:49 JST

## Summary

- baseline: `51eabd0d1e4466eb427394912eddb6f7a9d0cafb` (`2024-06-11`)
- accepted candidate: `24a4123fc949aad0c98d251b05c8ba2b21a9b931`
- candidate date: `2024-06-18T18:50:15Z`
- subject: `Revert "wgsl: Mark more variables as const/immutable"`
- result: accepted

## Candidate Selection

cycle 024 の next note に従い、accepted baseline から 1-week/2-week 候補を比較した。2-week 候補で public Vulkan header removal と追加の Graphite precompile API が入るため、cycle 025 は 1-week 候補を採用した。

| candidate | result | note |
| --- | --- | --- |
| `24a4123fc949aad0c98d251b05c8ba2b21a9b931` | accepted | 1-week candidate; 73 commits, `include`/`modules` drift 7 files |
| `a8c2acc3903806ff36800a67f5e60dba84265fd3` | deferred | 2-week candidate; 138 commits from cycle 024 baseline, `include`/`modules` drift 19 files and Vulkan public header removals |

accepted candidate の `include` / `modules` diff は `7 files changed, 536 insertions(+), 2 deletions(-)`。`DEPS` / `include` / `modules` / `src` の source/dependency drift は `91 files changed, 4421 insertions(+), 3297 deletions(-)`。

2-week 候補は cycle 025 accepted candidate から 65 commits 先で、追加差分は `include`/`modules` 12 files、`include/gpu/vk/GrVkBackendContext.h` / `GrVkExtensions.h` / `GrVkMemoryAllocator.h` の削除と `include/gpu/graphite/precompile/PrecompileColorFilter.h` の追加を含む。次 cycle で個別 routing する。

## Coverage And Routing

Initial candidate coverage:

- `covered 2814`
- `false_positive 277`
- `na 204`
- `no_public_methods_found 105`
- `split_covered 33`
- `missing 29`

Initial stale C API report:

- `stale_capi 0`
- `signature_changed_review 0`

Final coverage:

- `covered 2814`
- `false_positive 286`
- `na 224`
- `no_public_methods_found 105`
- `split_covered 33`
- `missing 0`
- `deferred 0`
- `partial 0`
- `overcovered 0`

Final stale C API report:

- `stale_capi 0`
- `signature_changed_review 0`

Routing:

- `cycle025_graphite_precompile_design_required`: `PaintOptions` setters/getters and `PrecompileShader` wrapper helpers are public, but `SkSpan` array marshalling, `sk_sp` retention rules, opaque ownership, and `RESKIA_ENABLE_GPU_GRAPHITE` backend policyを決める必要があるため `na` とした。
- `cycle025_graphite_precompile_lifetime`: `PaintOptions` constructor/copy/destructor rows are value/lifetime management rowsとして C ABI 直接露出対象外にした。
- `cycle025_graphite_precompile_internal`: `priv()` accessors are internal/private windowなので `false_positive` とした。
- `optional-backend`: Graphite precompile implementationは optional Graphite backend の内部実装として同期した。portable C ABI の新規 entry point は追加していない。

## Source/Header Sync

candidate から `DEPS`、public `include`、`modules/sksg`、`src` の mirrored diff を同期した。

主な同期:

- public Graphite precompile headers: `PaintOptions.h`, `Precompile.h`, `PrecompileBase.h`, `PrecompileBlender.h`, `PrecompileShader.h`。
- `SkPaint` の Graphite precompile helper surface。
- `SkSGRenderNode` の render-node update。
- codec/core updates: EXIF/TIFF/JPEG multi-picture、analytic edge、blur、mask gamma、path/scaler/image-filter updates。
- Graphite precompile implementation split and private header moves: `src/gpu/graphite/precompile/*`, `PrecompileInternal.h`。
- Metal/Dawn/Graphite backend source sync。
- SkSL module data split: `SkSLModuleData.h`, `SkSLModuleDataDefault.cpp`, generated SkSL updates。
- fontations source sync and `SkFontMgr_fontations_empty.h` removal。

CMake/source-list:

- Added `src/sksl/SkSLModuleDataDefault.cpp` to `cmake/reskia/sources-core.cmake`.
- Added `src/gpu/graphite/precompile/*.c|*.cc|*.cpp` to the Graphite source glob.
- Did not build `src/sksl/SkSLModuleDataFile.cpp`; it depends on `tools/SkGetExecutablePath.h`, outside Reskia's CMake build surface.
- Fixed shaper C API source gating by moving `capi/sk_text_blob_builder_run_handler.cpp` under `TARGET skshaper`, because `test_shaper_capi_smoke` and `capi/sk_shaper.cpp` use that C API outside `RESKIA_ENABLE_SKPARAGRAPH`.
- Reskia does not track GN/Bazel metadata in the CMake build surface.

## C API Catch-up

Implemented:

- No new C API entry points were added.
- `sk_text_blob_builder_run_handler.cpp` CMake gating was corrected so existing shaper C API links whenever `skshaper` is enabled.

Removed because upstream removed:

- No public C API removal was required.

Classified:

- 29 Graphite precompile rows were classified in `public-api-phase-34-upgrade-probe-overrides.csv`: 20 `na`, 9 `false_positive`.

## Verification

| gate | result | command/log |
| --- | --- | --- |
| initial coverage regression | pass after routing | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output .tmp/skia-upgrade-cycle-025/candidate-public-api-coverage-matrix.csv --previous-matrix docs/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --stale-output .tmp/skia-upgrade-cycle-025/candidate-stale-capi.csv` |
| canonical coverage regeneration | pass | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output docs/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --previous-matrix .tmp/skia-upgrade-cycle-025/previous-public-api-coverage-matrix.csv --stale-output docs/plans/c-binding-remediation/checklists/public-api-stale-capi.csv` |
| stale C API report | pass | canonical stale report has 0 rows; `stale_capi 0`, `signature_changed_review 0` |
| prebuilt configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle025-prebuilt -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle025-prebuilt -j 8` |
| source configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle025-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle025-source -j 8` |
| GPU smoke configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle025-gpu-smoke -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_GPU_GANESH=ON -DRESKIA_ENABLE_GPU_GRAPHITE=ON -DRESKIA_ENABLE_GPU_METAL=ON`; `cmake --build skia/cmake-build-codex-cycle025-gpu-smoke -j 8` |
| GPU smoke ctest | pass | `ctest --test-dir skia/cmake-build-codex-cycle025-gpu-smoke -R 'c_skia_gpu_(context|surface)_capi_smoke' --output-on-failure` -> 2/2 pass |
| source SVG/provider/text smoke build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle025-source-smoke -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_SKPARAGRAPH=ON`; `cmake --build skia/cmake-build-codex-cycle025-source-smoke -j 8` |
| source SVG/provider/text smoke ctest | pass | `ctest --test-dir skia/cmake-build-codex-cycle025-source-smoke -R 'c_skia_(shaping_smoke|shaper_capi_smoke|svg_dom_capi_smoke|skresources_capi_smoke|unicode_capi_smoke|skparagraph_smoke|paragraph_builder_capi_smoke|path_invalid_input_smoke)' --output-on-failure` -> 8/8 pass |
| Skottie/SKSG optional smoke build | pass after CMake gating fix | `cmake -S skia -B skia/cmake-build-codex-cycle025-skottie-sksg-smoke -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_SKOTTIE=ON -DRESKIA_ENABLE_SKSG=ON`; `cmake --build skia/cmake-build-codex-cycle025-skottie-sksg-smoke -j 8` |
| Skottie/SKSG optional smoke ctest | pass | `ctest --test-dir skia/cmake-build-codex-cycle025-skottie-sksg-smoke -R 'c_skia_(skottie_smoke|sksg_smoke|sksg_capi_smoke)' --output-on-failure` -> 3/3 pass |

Notes:

- Initial prebuilt build failed when `SkSLModuleDataFile.cpp` was included; it depends on `tools/SkGetExecutablePath.h`. The file remains mirrored but is not part of Reskia's CMake source list.
- Skottie/SKSG build initially exposed existing CMake gating drift for `sk_text_blob_builder_run_handler.cpp`; moving it under `TARGET skshaper` fixed the link.
- Existing warnings observed: Metal `fastMathEnabled` deprecation, `stb_image_write.h` `sprintf` deprecation, and prebuilt `libsvg.a` macOS deployment-version linker warning.

## Lock Update

`vendor/skia-source.lock`:

- `SKIA_REF="24a4123fc949aad0c98d251b05c8ba2b21a9b931"`
- `SKIA_BASELINE_DATE="2024-06-18"`

`vendor/skia-upstream` と `vendor/skia-upstream-candidate` は accepted candidate `24a4123fc949aad0c98d251b05c8ba2b21a9b931` に同期済み。

## Next Cycle Notes

- 次サイクルでは accepted baseline `24a4123fc949aad0c98d251b05c8ba2b21a9b931` から再比較する。
- 1-week 相当の次候補 `a8c2acc3903806ff36800a67f5e60dba84265fd3` は 65 commits、`include`/`modules` drift 12 files。
- 次候補は `include/gpu/vk/GrVkBackendContext.h` / `GrVkExtensions.h` / `GrVkMemoryAllocator.h` の upstream removal と、`include/gpu/graphite/precompile/PrecompileColorFilter.h` 追加を含む。Vulkan public header removal に対応する stale C API と Graphite precompile color-filter routing を先に確認する。
