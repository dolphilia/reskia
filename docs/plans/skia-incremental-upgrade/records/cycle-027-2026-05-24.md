# Skia Incremental Upgrade Cycle 027

作業日時: 2026-05-24 10:28:14 JST

## Summary

- baseline: `a8c2acc3903806ff36800a67f5e60dba84265fd3` (`2024-06-25`)
- accepted candidate: `7c69f39fa85b3cca07c7d433a396011e01c88f34`
- candidate date: `2024-07-02T21:38:47Z`
- subject: `Remove staging gni file groups for sksl`
- result: accepted

## Candidate Selection

cycle 026 の next note に従い、Graphite `PrecompileImageFilter` / `PrecompileMaskFilter` public API を含む 1-week 候補を採用した。

| candidate | result | note |
| --- | --- | --- |
| `7c69f39fa85b3cca07c7d433a396011e01c88f34` | accepted | 88 commits; `include`/`modules` drift 7 files |
| `c73cff97952aa15e01985a35e5c6575b4eb50454` | deferred | 154 commits from cycle 026 baseline; 66 commits from accepted candidate; `include`/`modules` drift 105 files from cycle 026 baseline, mostly Bazel metadata churn |

accepted candidate の `include` / `modules` diff は `7 files changed, 183 insertions(+)`。`DEPS` / `gn` / `bazel` / `include` / `modules` / `src` の source/dependency drift は `91 files changed, 2802 insertions(+), 2455 deletions(-)`。

主要 public header:

- `include/core/SkImageInfo.h`
- `include/gpu/graphite/Recorder.h`
- `include/gpu/graphite/precompile/PaintOptions.h`
- `include/gpu/graphite/precompile/PrecompileImageFilter.h`
- `include/gpu/graphite/precompile/PrecompileMaskFilter.h`
- `include/gpu/vk/BUILD.bazel`
- `include/private/gpu/vk/BUILD.bazel`

## Coverage And Routing

Initial candidate coverage:

- `covered 2816`
- `false_positive 286`
- `na 225`
- `no_public_methods_found 106`
- `split_covered 33`
- `missing 4`

Initial stale C API report:

- `stale_capi 0`
- `signature_changed_review 0`

Final coverage:

- `covered 2817`
- `false_positive 289`
- `na 225`
- `no_public_methods_found 106`
- `split_covered 33`
- `missing 0`
- `deferred 0`
- `partial 0`
- `overcovered 0`

Final stale C API report:

- `stale_capi 0`
- `signature_changed_review 0`

Routing:

- `gpu-ganesh-graphite`: `Recorder::maxTextureSize` was implemented in C API as `Graphite_Recorder_maxTextureSize`.
- `design-required`: Graphite precompile image/mask filter factories remain covered by the existing precompile ownership/design policy.
- `generator-noise`: `PrecompileImageFilter` destructor and `priv()` accessors were classified as lifetime/internal surface.
- `removed-upstream`: `src/gpu/ganesh/GrRenderTargetContext.h` was removed upstream. stale C API report stayed 0, so no C API deletion was required.

## Source/Header Sync

Synced candidate diff from `DEPS`, public `include`, mirrored `src`, tracked `BUILD.bazel` metadata under mirrored trees, and tracked `gn/gn_to_cmake.py`.

Main sync:

- Core `SkImageInfo` helpers and YUV math updates.
- Graphite recorder `maxTextureSize`, precompile image/mask filter headers and implementation split, shader dictionary/codegen updates, renderer and pipeline updates.
- Ganesh/Graphite Metal and Vulkan implementation updates.
- Vulkan AMD memory allocator move to `src/gpu/vk/vulkanmemoryallocator/`.
- Text GPU, CoreText, SkSL generated source, and utility updates.

CMake/source-list:

- Added `src/gpu/vk/vulkanmemoryallocator/*` to the Vulkan GPU source glob so the upstream AMD memory allocator move is built when Vulkan is enabled.
- Existing Graphite precompile glob picked up `PrecompileImageFilter.cpp` and `PrecompileMaskFilter.cpp`.
- Root `bazel/` and untracked root `gn/*.gni` metadata were intentionally not added to Reskia's tracked mirror surface.

## C API Catch-up

Implemented:

- `Graphite_Recorder_maxTextureSize`

Removed because upstream removed:

- No public C API removal was required.

Classified:

- `PrecompileImageFilter::~PrecompileImageFilter` as `false_positive` lifetime surface.
- `PrecompileImageFilter::priv()` overloads as `false_positive` internal surface.

## Verification

| gate | result | command/log |
| --- | --- | --- |
| initial coverage regression | pass after routing | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output .tmp/skia-upgrade-cycle-027/candidate-public-api-coverage-matrix.csv --previous-matrix docs/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --stale-output .tmp/skia-upgrade-cycle-027/candidate-stale-capi.csv` |
| canonical coverage regeneration | pass | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output docs/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --previous-matrix .tmp/skia-upgrade-cycle-027/previous-public-api-coverage-matrix.csv --stale-output docs/plans/c-binding-remediation/checklists/public-api-stale-capi.csv` |
| stale C API report | pass | canonical stale report has 0 rows |
| prebuilt configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle027-prebuilt -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle027-prebuilt -j 8` |
| source configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle027-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle027-source -j 8` |
| GPU smoke build/ctest | pass | `cmake -S skia -B skia/cmake-build-codex-cycle027-gpu-smoke -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_GPU_GANESH=ON -DRESKIA_ENABLE_GPU_GRAPHITE=ON -DRESKIA_ENABLE_GPU_METAL=ON`; `cmake --build ...`; `ctest --test-dir skia/cmake-build-codex-cycle027-gpu-smoke -R 'c_skia_gpu_(context|surface)_capi_smoke' --output-on-failure` -> 2/2 pass |
| source SVG/provider/text smoke build/ctest | pass | `cmake -S skia -B skia/cmake-build-codex-cycle027-source-smoke -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_SKPARAGRAPH=ON`; `cmake --build ...`; `ctest --test-dir skia/cmake-build-codex-cycle027-source-smoke -R 'c_skia_(shaping_smoke|shaper_capi_smoke|svg_dom_capi_smoke|skresources_capi_smoke|unicode_capi_smoke|skparagraph_smoke|paragraph_builder_capi_smoke|path_invalid_input_smoke)' --output-on-failure` -> 8/8 pass |

Notes:

- Existing warnings observed: Metal `fastMathEnabled` deprecation, `stb_image_write.h` `sprintf` deprecation, and prebuilt `libsvg.a` macOS deployment-version linker warning.

## Lock Update

`vendor/skia-source.lock`:

- `SKIA_REF="7c69f39fa85b3cca07c7d433a396011e01c88f34"`
- `SKIA_BASELINE_DATE="2024-07-02"`

`vendor/skia-upstream` と `vendor/skia-upstream-candidate` は accepted candidate `7c69f39fa85b3cca07c7d433a396011e01c88f34` に同期済み。

## Next Cycle Notes

- 次サイクルでは accepted baseline `7c69f39fa85b3cca07c7d433a396011e01c88f34` から再比較する。
- 1-week 相当候補は `c73cff97952aa15e01985a35e5c6575b4eb50454`。66 commits; `include`/`modules` drift は `100 files changed, 115 insertions(+), 366 deletions(-)` で Bazel metadata churn が大きい。
- public API 注目点は `PrecompileShader`, GL helper, private Graphite context option removal。
