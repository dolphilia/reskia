# Skia Incremental Upgrade Cycle 019

作業日時: 2026-05-23 16:27:05 JST

## Summary

- baseline: `b159229f2174800f6655f7b7dbba01d7bd3d5d48` (`2024-04-15`)
- accepted candidate: `22d1130a2ba22beb7156d5578f1a9942d1e88bcb`
- candidate date: `2024-04-22T05:13:21Z`
- subject: `Roll SK Tool from b7cc986dba6d to 2bc8bf46657d`
- result: accepted

## Candidate Selection

1-week date-end candidate を採用した。2-week/3-week 候補は include/modules と broad source の drift が広がり、cycle 019 では `SkPath` C API 追従、削除済み remotable font manager 周辺の整理、低リスクな source/header sync に閉じられる 1-week 幅を選んだ。

| candidate | result | note |
| --- | --- | --- |
| `22d1130a2ba22beb7156d5578f1a9942d1e88bcb` | accepted | 1-week candidate; 107 commits |
| `8ced43c89f32b8defb8ea6e9c3316f44a7a02382` | deferred | 2-week candidate; include/modules drift が増えるため次 cycle 以降で再評価 |
| `4754c3fd6b73f8693be8951a51f1a5b0c0ff6293` | deferred | 3-week candidate; Vulkan roll と broad source drift がさらに広がるため deferred |

1-week candidate の `include` / `modules` diff は `60 files changed, 631 insertions(+), 682 deletions(-)`。

## Coverage And Routing

Final coverage:

- `covered 2797`
- `false_positive 275`
- `na 203`
- `no_public_methods_found 106`
- `split_covered 33`
- `missing 0`
- `deferred 0`
- `partial 0`
- `overcovered 0`

Stale/signature review:

- stale C API report は `stale_capi 0`。
- `signature_changed_review` は 7 件。いずれも C ABI 互換と判断した。
  - `SkM44::isFinite`, `SkMatrix::isFinite`, `SkPoint3::isFinite`, `SkRect::isEmpty`: inline implementation/body の変更のみで C ABI は bool return のまま。
  - `SkPath::offset`, `SkPath::transform`: in-place overload が `SkPath&` return になったが、既存 C API は void wrapper で return を破棄するため ABI 互換。
  - `SkStrokeRec::setResScale`: inline body の変更のみで C ABI は void wrapper のまま。

Routing:

- path: `SkPath::snapshot` / `SkPath::detach` を C API 化し、invalid input smoke に null/handle round-trip を追加。
- stale C API: upstream で削除された `SkRemotableFontMgr` / `SkFontMgr_Indirect` 系 C API と smoke を削除。
- gpu: deleted wrapper header `include/gpu/GrBackendDrawableInfo.h` に代えて Vulkan/Ganesh header を直接 include。
- shaper: fallback font manager non-null assertion に合わせ、legacy `MakeFontMgrRunIterator` の null fallback を `SkFontMgr::RefEmpty()` に正規化。C API factory helpers も null manager を default fallback に寄せた。
- encode: cycle 016 の ICC 互換 helper 定義を source sync 後に再適用。

## Source/Header Sync

candidate から `include`、`src`、`modules/skcms`、`modules/skottie`、`modules/skparagraph`、`modules/skshaper`、`modules/svg`、`gn`、`DEPS` の対象差分を同期した。GN/DEPS は参照用として同期し、CMake build の入口は従来通り `skia/CMakeLists.txt`。

主な同期:

- `include/core/SkArc.h`, `SkCanvas.h`, `SkM44.h`, `SkMatrix.h`, `SkPath.h`
- `include/gpu/ganesh/gl/*/GrGLMake*Interface.h`
- `include/private/base/SkASAN.h`
- `src/base/SkFloatBits.h`
- `modules/skottie/src/*`
- `modules/skparagraph/src/*`
- `modules/skshaper/src/SkShaper.cpp`
- `src/gpu/ganesh/*`, `src/gpu/graphite/*`

## C API Catch-up

- Added `SkPath_snapshot`.
- Added `SkPath_detach`.
- Removed deleted upstream `sk_remotable_font_mgr` C API surface and `test_remotable_font_mgr_utils_smoke`.

## Build Fixes

- Removed deleted upstream remotable/indirect font manager sources from CMake source lists and tests.
- Added newly required synced headers `include/private/base/SkASAN.h` and `src/base/SkFloatBits.h`.
- Reintroduced `SkICCFloatXYZD50ToGrid16Lab` and `SkICCFloatToTable16` compatibility definitions after source sync.
- Normalized null shaper fallback font managers to `SkFontMgr::RefEmpty()` in C API creation helpers and both legacy `SkShaper::MakeFontMgrRunIterator` overloads.
- Updated shaping smoke to pass an explicit font manager into `SkShaper::Make`.

## Verification

| gate | result | command/log |
| --- | --- | --- |
| coverage regeneration | pass | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output .tmp/skia-upgrade-cycle-019/after-capi-public-api-coverage-matrix.csv --previous-matrix docs/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --stale-output .tmp/skia-upgrade-cycle-019/after-capi-stale-capi.csv` |
| stale C API report | pass | `stale_capi 0`; `signature_changed_review 7` reviewed as ABI-compatible |
| prebuilt configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle019-prebuilt -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle019-prebuilt -j 8` |
| source configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle019-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle019-source -j 8` |
| GPU smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle019-gpu-smoke -R 'c_skia_gpu_(context|surface)_capi_smoke' --output-on-failure` -> 2/2 pass |
| source SVG/provider/text smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle019-source-smoke -R 'c_skia_(shaping_smoke|shaper_capi_smoke|svg_dom_capi_smoke|skresources_capi_smoke|unicode_capi_smoke|skparagraph_smoke|paragraph_builder_capi_smoke|path_invalid_input_smoke)' --output-on-failure` -> 8/8 pass |
| Skottie/SKSG optional smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle019-skottie-sksg-smoke -R 'c_skia_(skottie_smoke|sksg_smoke|sksg_capi_smoke)' --output-on-failure` -> 3/3 pass |
| diff check | pass | `git diff --check` -> CSV CRLF replacement warning only |

Notes:

- prebuilt/GPU link では `libsvg.a` の macOS 15.1 object を macOS 15.0 target に link する既存 warning が出る。
- GPU smoke の configure では Metal `fastMathEnabled` deprecation warning が出るが、対象 smoke は pass。

## Lock Update

`vendor/skia-source.lock`:

- `SKIA_REF="22d1130a2ba22beb7156d5578f1a9942d1e88bcb"`
- `SKIA_BASELINE_DATE="2024-04-22"`

`vendor/skia-upstream` と `vendor/skia-upstream-candidate` は accepted candidate `22d1130a2ba22beb7156d5578f1a9942d1e88bcb` に同期済み。

## Next Cycle Notes

- 次サイクルでは accepted baseline `22d1130a2ba22beb7156d5578f1a9942d1e88bcb` から 1-week candidate を第一候補として選ぶ。
- 2-week/3-week 候補は diff と coverage regression が管理可能な場合のみ採用する。
