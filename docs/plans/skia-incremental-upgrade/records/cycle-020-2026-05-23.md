# Skia Incremental Upgrade Cycle 020

作業日時: 2026-05-23 16:38:28 JST

## Summary

- baseline: `22d1130a2ba22beb7156d5578f1a9942d1e88bcb` (`2024-04-22`)
- accepted candidate: `a6bc04f984d25bbce342d33ee469b15e93e37698`
- candidate date: `2024-04-30T21:28:14Z`
- subject: `Escape unicode sequence`
- result: accepted

## Candidate Selection

1-week date-end candidate を採用した。2-week/3-week 候補は PDF、TextureInfo、SkUnicode、SVG filter、Dawn 周辺の drift が広がり、cycle 020 では `SkArc` / `SkPath::isArc` C API 追従と低リスクな source/header sync に閉じられる 1-week 幅を選んだ。

| candidate | result | note |
| --- | --- | --- |
| `a6bc04f984d25bbce342d33ee469b15e93e37698` | accepted | 1-week candidate; 118 commits |
| `423b224869102f5462a6f15b6a1a558f07f72739` | deferred | 2-week candidate; include/modules と dependency/source-list drift が増えるため次 cycle 以降で再評価 |
| `d0d87c26b4899728b6e344d68843cae9f8c86c18` | deferred | 3-week candidate; public/module drift がさらに広がるため deferred |

1-week candidate の `include` / `modules` diff は `105 files changed, 658 insertions(+), 91 deletions(-)`。

## Coverage And Routing

Final coverage:

- `covered 2806`
- `false_positive 275`
- `na 203`
- `no_public_methods_found 105`
- `split_covered 33`
- `missing 0`
- `deferred 0`
- `partial 0`
- `overcovered 0`

Stale/signature review:

- stale C API report は `stale_capi 0`。
- initial probe の `signature_changed_review` は `SkCanvas::drawArc` 1 件。inline `SkArc` overload body が `arc.isWedge()` を使う形へ変わったが、既存 `SkCanvas_drawArc` は rect/start/sweep/useCenter overload を包む void wrapper のままで C ABI 互換と判断した。

Routing:

- core/path: `SkArc` と `SkPath::isArc` を C API 化。
- Dawn: `DawnTextureInfo` の新規 WebGPU/Dawn constructor は `RESKIA_ENABLE_GPU_DAWN` と Dawn/WebGPU runtime 型が必要な optional backend API のため `na` として記録。
- SVG: `SkSVGEllipse` の `SVG_ATTR` macro signature 変更は standalone public C ABI method ではないため `false_positive` として記録。

## Source/Header Sync

candidate から `DEPS`、`gn`、`include`、`modules`、`src` の対象差分を同期した。GN/DEPS は参照用として同期し、CMake build の入口は従来通り `skia/CMakeLists.txt`。

主な同期:

- `include/core/SkArc.h`, `SkCanvas.h`, `SkPath.h`, `SkScalar.h`
- `modules/svg/src/SkSVGRectPriv.h` と SVG 実装更新
- `src/base/SkFixedArray.h`
- Graphite の `AttachmentTypes` から `RenderPassDesc` への置換
- Graphite blur 実装の `RectBlur*` から `AnalyticBlur*` への置換
- `DEPS` / `gn` / tracked Bazel metadata の候補 commit 追従

## C API Catch-up

- Added `skia/capi/sk_arc.h` / `skia/capi/sk_arc.cpp`。
- Added `SkArc_new`, `SkArc_newCopy`, `SkArc_delete`。
- Added `SkArc_oval`, `SkArc_startAngle`, `SkArc_sweepAngle`, `SkArc_isWedge`。
- Added `SkArc_Make`, `SkArc_MakeWithType`。
- Added `SkPath_isArc`, with optional null output arc.
- Added `capi/sk_arc.cpp` to `cmake/reskia/sources-capi.cmake`。

## Build Fixes

- source/header sync 後に新規 private headers/source を復元した。
- SVG private header `SkSVGRectPriv.h` は `.cpp` include 用で、CMake source list への追加は不要と確認した。
- GPU/Graphite source は glob で拾われるため、rename 追従に個別 source-list 変更は不要だった。
- source smoke では `test_shaper_capi_smoke` の link に `SkTextBlobBuilderRunHandler` が必要なため、対象 smoke build は `RESKIA_ENABLE_SKPARAGRAPH=ON` で構成した。
- `RESKIA_BUILD_TESTS=ON` の full all-target build は既存の legacy `test_c_skia` 型エラーで停止するため、cycle gate は対象 smoke target に限定した。

## Verification

| gate | result | command/log |
| --- | --- | --- |
| coverage regeneration | pass | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output .tmp/skia-upgrade-cycle-020/final-public-api-coverage-matrix.csv --previous-matrix docs/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --stale-output .tmp/skia-upgrade-cycle-020/final-stale-capi.csv` |
| stale C API report | pass | `stale_capi 0`; final stale report empty |
| prebuilt configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle020-prebuilt -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle020-prebuilt -j 8` |
| source configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle020-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle020-source -j 8` |
| GPU smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle020-gpu-smoke -R 'c_skia_gpu_(context|surface)_capi_smoke' --output-on-failure` -> 2/2 pass |
| source SVG/provider/text smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle020-source-smoke -R 'c_skia_(shaping_smoke|shaper_capi_smoke|svg_dom_capi_smoke|skresources_capi_smoke|unicode_capi_smoke|skparagraph_smoke|paragraph_builder_capi_smoke|path_invalid_input_smoke)' --output-on-failure` -> 8/8 pass |
| Skottie/SKSG optional smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle020-skottie-sksg-smoke -R 'c_skia_(skottie_smoke|sksg_smoke|sksg_capi_smoke)' --output-on-failure` -> 3/3 pass |
| diff check | pass | `git diff --check` -> CSV CRLF replacement warning only |

Notes:

- prebuilt/GPU link では `libsvg.a` の macOS 15.1 object を macOS 15.0 target に link する既存 warning が出る。
- GPU build では Metal `fastMathEnabled` deprecation warning が出るが、対象 smoke は pass。

## Lock Update

`vendor/skia-source.lock`:

- `SKIA_REF="a6bc04f984d25bbce342d33ee469b15e93e37698"`
- `SKIA_BASELINE_DATE="2024-04-30"`

`vendor/skia-upstream` と `vendor/skia-upstream-candidate` は accepted candidate `a6bc04f984d25bbce342d33ee469b15e93e37698` に同期済み。

## Next Cycle Notes

- 次サイクルでは accepted baseline `a6bc04f984d25bbce342d33ee469b15e93e37698` から 1-week candidate を第一候補として選ぶ。
- 2-week/3-week 候補は PDF、TextureInfo、SkUnicode、SVG filter、Dawn 周辺の drift が管理可能な場合のみ採用する。
