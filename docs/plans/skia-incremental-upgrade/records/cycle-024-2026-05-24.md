# Skia Incremental Upgrade Cycle 024

作業日時: 2026-05-24 07:58:57 JST

## Summary

- baseline: `73e53abdf54f56fe30d60dba6e62e19597fa2618` (`2024-06-04`)
- accepted candidate: `51eabd0d1e4466eb427394912eddb6f7a9d0cafb`
- candidate date: `2024-06-11T20:30:41Z`
- subject: `SkPath: optimize reading paths`
- result: accepted

## Candidate Selection

cycle 023 の next note に従い、accepted baseline から 1-week/2-week/3-week 候補を比較した。

| candidate | result | note |
| --- | --- | --- |
| `51eabd0d1e4466eb427394912eddb6f7a9d0cafb` | accepted | 1-week candidate; 93 commits, `include`/`modules` drift 17 files |
| `24a4123fc949aad0c98d251b05c8ba2b21a9b931` | deferred | 2-week candidate; 166 commits from cycle 023 baseline, but adds new public Graphite precompile headers |
| `a8c2acc3903806ff36800a67f5e60dba84265fd3` | deferred | 3-week candidate; broader public/header and source drift |

accepted candidate の `include` / `modules` diff は `17 files changed, 149 insertions(+), 124 deletions(-)`。`DEPS` / `gn` / `include` / `modules` / `src` の source/dependency drift は `69 files changed, 2178 insertions(+), 1177 deletions(-)`。

2-week 候補との差分では `include/gpu/graphite/precompile/*` が public surface としてまとまって追加されるため、cycle 024 は 1-week 幅に戻して低リスク sync を優先した。

## Coverage And Routing

Initial candidate coverage:

- `covered 2814`
- `false_positive 277`
- `na 204`
- `no_public_methods_found 105`
- `split_covered 33`
- `missing 0`

Initial stale C API report:

- `stale_capi 0`
- `signature_changed_review 0`

Final coverage:

- `covered 2814`
- `false_positive 277`
- `na 204`
- `no_public_methods_found 105`
- `split_covered 33`
- `missing 0`
- `deferred 0`
- `partial 0`
- `overcovered 0`

Final stale C API report:

- `stale_capi 0`
- `signature_changed_review 0`

Routing:

- `core-codec-low-risk`: `SkPath`, `SkPathRef`, `SkJpegMultiPicture`, PNG BGRA10XR encode path, `SkBlurEngine` split.
- `gpu-ganesh-graphite`: Ganesh/Graphite blur utility adoption, Metal/Vulkan/Dawn type sync, Graphite factory/precompile implementation sync.
- `optional-backend`: Dawn/Vulkan/Metal changes follow existing optional backend routing; no new portable C ABI.
- `generator-noise`: no new overrides required.
- `removed-upstream`: no public C API removal required.

## Source/Header Sync

candidate から `DEPS`、`include`、`modules/skcms`、`src` の mirrored diff を同期した。

主な同期:

- Core blur refactor: `SkBlurEngine.cpp` / `.h` and `SkImageFilterTypes` integration.
- Path serialization and `SkPath` optimization updates.
- JPEG multi-picture and PNG BGRA10XR encode updates.
- Ganesh/Graphite blur utility and shader generation updates.
- Metal/Vulkan/Dawn backend type/header sync.
- `skcms` roll to `bc467ef5ef35`.

CMake/source-list:

- Added `src/core/SkBlurEngine.cpp` to `cmake/reskia/sources-core.cmake`.
- Reskia does not track `gn/*.gni` or Bazel metadata in the CMake build surface; generated untracked metadata from the mirror sync was not added.

## C API Catch-up

Implemented:

- No new C API entry points were required.

Removed because upstream removed:

- No public C API removal was required.

Classified:

- No new `na` or `false_positive` override was required.

## Verification

| gate | result | command/log |
| --- | --- | --- |
| coverage regeneration | pass | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output .tmp/skia-upgrade-cycle-024/final-public-api-coverage-matrix.csv --previous-matrix docs/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --stale-output .tmp/skia-upgrade-cycle-024/final-stale-capi.csv` |
| stale C API report | pass | after canonical matrix refresh, stale report header only; `stale_capi 0`, `signature_changed_review 0` |
| prebuilt configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle024-prebuilt -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle024-prebuilt -j 8` |
| source configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle024-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle024-source -j 8` |
| GPU smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle024-gpu-smoke -R 'c_skia_gpu_(context|surface)_capi_smoke' --output-on-failure` -> 2/2 pass |
| GPU smoke all-target build | pass | after legacy `test_c_skia` C API type catch-up, `cmake --build skia/cmake-build-codex-cycle024-gpu-smoke -j 8` |
| legacy `c_skia_test` | pass | `ctest --test-dir skia/cmake-build-codex-cycle024-gpu-smoke -R '^c_skia_test$' --output-on-failure` -> 1/1 pass |
| GPU smoke full ctest | pass | after smoke expectation catch-up, `ctest --test-dir skia/cmake-build-codex-cycle024-gpu-smoke --output-on-failure` -> 88/88 pass |
| source SVG/provider smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle024-source-smoke -R 'c_skia_(shaping_smoke|shaper_capi_smoke|svg_dom_capi_smoke|skresources_capi_smoke|unicode_capi_smoke|skparagraph_smoke|paragraph_builder_capi_smoke|path_invalid_input_smoke)' --output-on-failure` -> 8/8 pass |
| Skottie/SKSG optional smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle024-skottie-sksg-smoke -R 'c_skia_(skottie_smoke|sksg_smoke|sksg_capi_smoke)' --output-on-failure` -> 3/3 pass |

Notes:

- GPU build emits the existing Metal `fastMathEnabled` deprecation warning; target smoke passes.
- Legacy `test_c_skia` compile drift was fixed by updating `skia/test/test.cpp` to current opaque C API types and skipping file-dependent cases when optional local fixtures are absent.
- Smoke expectations for `SkFont_setTypeface(0)` and static `SkUnicode_extractBidi` now match current C API behavior. Full GPU smoke `ctest` passes.

## Lock Update

`vendor/skia-source.lock`:

- `SKIA_REF="51eabd0d1e4466eb427394912eddb6f7a9d0cafb"`
- `SKIA_BASELINE_DATE="2024-06-11"`

`vendor/skia-upstream` と `vendor/skia-upstream-candidate` は accepted candidate `51eabd0d1e4466eb427394912eddb6f7a9d0cafb` に同期済み。

## Next Cycle Notes

- 次サイクルでは accepted baseline `51eabd0d1e4466eb427394912eddb6f7a9d0cafb` から再比較する。
- 1-week 候補 `24a4123fc949aad0c98d251b05c8ba2b21a9b931` は 73 commits、`include`/`modules` drift 7 files、ただし Graphite precompile public headers が大きい。
- 2-week 候補 `a8c2acc3903806ff36800a67f5e60dba84265fd3` は 138 commits、`include`/`modules` drift 19 files。
- 次 cycle は Graphite precompile public API の routing を先に行い、C ABI 露出対象にするか optional/design-required として分類する。
