# Skia Incremental Upgrade Cycle Record

作成時刻: 2026-05-31 16:03:07 JST

## Summary

- cycle: 063
- branch: `incremental-upgrade`
- baseline commit: `df39eefbeef3d86d8237d24692208b9ba78d1c5d`
- candidate commit: `fb7334edc4de5833a67324e6bca1a9143dd4d607`
- candidate date: `2025-08-01T00:36:20-07:00`
- candidate subject: `Roll ANGLE from d94f5aa38f2a to 2ef85c2418f4 (8 revisions)`
- result: `accepted`

## Baseline Check

- start time: `2026-05-31 15:37:18 JST`
- `vendor/skia-source.lock`: `SKIA_REF="df39eefbeef3d86d8237d24692208b9ba78d1c5d"`, `SKIA_BASELINE_DATE="2025-07-18"`
- `vendor/skia-upstream` HEAD before probe: `df39eefbeef3d86d8237d24692208b9ba78d1c5d`
- coverage summary before probe: `missing 0` / `deferred 0` / `partial 0` / `overcovered 0`; stale report empty
- last known build/smoke baseline: cycle 062 prebuilt/source builds, GPU smoke, and source SVG/provider/text/path smoke passed

## Candidate Selection

| item | value |
| --- | --- |
| baseline to candidate commits | 165 |
| `include` / `modules` shortstat | 48 files changed, 597 insertions(+), 694 deletions(-) |
| `DEPS` / `gn` / `bazel` / `include` / `modules` / `src` drift | 217 files changed, 3189 insertions(+), 3549 deletions(-) |
| selected width | 2-week candidate |
| fallback candidates | 1-week `f248eba5e8d7d4828c0fd5390c5d5f29bb99f5be`; 2-week end `7ef888e30a28993395625a27a5db0458d4507b59`; 3-week `57f4880b53b5582f0a3717f3b044556104621dc3` |
| primary affected areas | Core path/matrix/rect API drift, Graphite capture hooks, codec/image/pdf/source drift, ANGLE/Dawn/Vulkan rolls |

The 2-week candidate was selected because it matched the standard cycle width and the public-header regression was small enough to close in-cycle. The 3-week candidate was held back because it expanded to 267 commits and materially larger include/modules drift.

## Public Header Delta

主要 header/source:

- `include/core/SkMatrix.h`
- `include/core/SkPath.h`
- `include/core/SkPathBuilder.h`
- `include/core/SkRect.h`
- `include/gpu/graphite/Context.h`

注目 API:

- `SkMatrix` gained static `PolyToPoly` and moved polygon APIs to `SkSpan`.
- `SkPath` gained raw-shape construction helpers through `SkPath::Raw`.
- `SkPathBuilder` gained relative raw path builder APIs, including `rMoveTo` and `addRaw`.
- `SkRect` gained optional-returning `Bounds` and value-returning `BoundsOrEmpty`.
- Graphite `Context` gained `startCapture` and `endCapture`.

## Coverage Regression

| status | initial candidate | final lock | note |
| --- | ---: | ---: | --- |
| `missing` | 8 | 0 | closed by C API catch-up |
| `deferred` | 0 | 0 |  |
| `partial` | 0 | 0 |  |
| `overcovered` | 0 | 0 |  |
| `covered` | 2901 | 2909 | eight new C ABI rows added |
| `split_covered` | 42 | 42 |  |
| `false_positive` | 294 | 294 |  |
| `na` | 255 | 255 |  |
| `no_public_methods_found` | 116 | 116 |  |
| `stale_capi` / `signature_changed_review` | 14 initial rows | 0 | official matrix updated after ABI review |

Initial missing rows:

- `SkMatrix::PolyToPoly`
- `SkPath::Raw`
- `SkPathBuilder::rMoveTo`
- `SkPathBuilder::addRaw`
- `SkRect::Bounds`
- `SkRect::BoundsOrEmpty`
- `Graphite Context::startCapture`
- `Graphite Context::endCapture`

Initial stale/signature rows were reviewed as ABI-compatible wrapper drift for existing `SkMatrix`, `SkPath`, and `SkPathBuilder` APIs after upstream moved several public methods to `SkSpan`, `SkPathVerb`, default `SkPathDirection::kDefault`, or optional-return forms.

## Routing

| area | rows/items | decision | owner note |
| --- | ---: | --- | --- |
| `core-codec-low-risk` | path/matrix/rect/read-buffer source and C ABI drift | sync + wrapper update | Synced tracked mirror files and added span/raw/optional compatibility wrappers |
| `gpu-ganesh-graphite` | Graphite context capture hooks and resource/source drift | sync + wrapper update | Added null-safe C ABI wrappers guarded by `SK_GRAPHITE` |
| `signature_changed_review` | 14 existing wrapper rows | ABI compatible | Kept C ABI surface stable while adapting implementations to upstream span/default/optional signatures |
| `dependency-drift` | `SkICC.cpp` new skcms helper calls | defer source sync | Reverted this file's candidate sync because current prebuilt skcms lacks the new HLG/PQ helper symbols |

## Source/Header Sync

- synced: tracked candidate changes under `skia/include/core`, `skia/include/gpu/graphite`, `skia/include/private/SkPathRef.h`, `skia/modules/skottie`, `skia/modules/sksg`, `skia/modules/svg`, `skia/src/codec`, `skia/src/core`, `skia/src/gpu`, `skia/src/image`, `skia/src/pdf`, `skia/src/ports`, `skia/src/shaders`, and `skia/src/utils`
- added: `skia/src/core/SkPathPriv.cpp`, `skia/src/core/SkPathRawShapes.cpp`, `skia/src/core/SkPathRawShapes.h`, `skia/src/capture/SkCaptureManager.cpp`, `skia/src/capture/SkCaptureManager.h`, `skia/src/capture/SkCaptureCanvas.cpp`, `skia/src/capture/SkCaptureCanvas.h`
- updated source list: `cmake/reskia/sources-core.cmake` now includes capture sources and new path raw/private sources
- intentionally not synced: untracked GN/Bazel/CanvasKit/PathKit metadata outside the current mirrored build surface
- dependency changes: no Reskia dependency mode policy changed in this cycle; `SkICC.cpp` candidate sync was deferred due current prebuilt skcms symbol drift

## C API Catch-up

- added: `SkMatrix_PolyToPoly`
- updated: `SkMatrix_setPolyToPoly` now calls the `SkSpan` overload while preserving the pointer/count C ABI
- added: `SkPath_Raw`, mapping byte verb arrays to local `SkPathVerb` storage before constructing the path
- added: `SkPathBuilder_rMoveTo`
- added: `SkPathBuilder_addRaw`, constructing local `SkPathRaw` span views from C arrays
- updated: `SkPathBuilder_verbs` to adapt the upstream `SkSpan<const SkPathVerb>` return to the existing `const uint8_t*` C ABI
- added: `SkRect_Bounds` and `SkRect_BoundsOrEmpty`
- added: `Graphite_Context_startCapture` and `Graphite_Context_endCapture`
- updated: `SkReadBuffer_readPath` to handle the upstream optional-returning `readPath`

## Verification

| gate | result | command/log |
| --- | --- | --- |
| baseline coverage | PASS | `.tmp/cycle-063-baseline-public-api-coverage-matrix.csv` -> `missing 0` / `deferred 0` / `partial 0` / `overcovered 0`; stale report empty |
| initial candidate coverage | FAIL as expected | `.tmp/skia-upgrade-cycle-063/candidate-public-api-coverage-matrix.csv` -> `missing 8`; stale report had 14 signature review rows |
| candidate coverage after C API catch-up | PASS | `.tmp/skia-upgrade-cycle-063/candidate-public-api-coverage-matrix-after-capi.csv` -> `missing 0` / `deferred 0` / `partial 0` / `overcovered 0` |
| final lock coverage | PASS | `.tmp/skia-upgrade-cycle-063/final-public-api-coverage-matrix.csv` -> `missing 0` / `deferred 0` / `partial 0` / `overcovered 0`; stale report empty |
| prebuilt configure/build | PASS | `cmake -S skia -B skia/cmake-build-codex-cycle063-prebuilt -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle063-prebuilt -j 8` |
| source configure/build | PASS | `cmake -S skia -B skia/cmake-build-codex-cycle063-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle063-source -j 8` |
| GPU smoke | PASS | GPU/Metal configure and build with `RESKIA_BUILD_TESTS=ON` passed; `ctest --test-dir skia/cmake-build-codex-cycle063-gpu-smoke -R 'c_skia_gpu_(context\|surface)_capi_smoke' --output-on-failure` -> 2/2 pass |
| source SVG/provider/text/path smoke | PASS | source/SkParagraph configure and build with `RESKIA_BUILD_TESTS=ON` passed; `ctest --test-dir skia/cmake-build-codex-cycle063-source-smoke -R 'c_skia_(shaping_smoke\|shaper_capi_smoke\|svg_dom_capi_smoke\|skresources_capi_smoke\|unicode_capi_smoke\|skparagraph_smoke\|paragraph_builder_capi_smoke\|path_invalid_input_smoke\|path_builder_invalid_input_smoke)' --output-on-failure` -> 9/9 pass |

Notes:

- prebuilt/GPU builds emitted known non-fatal macOS deployment-target warnings from prebuilt `libsvg.a`.
- builds emitted the existing `SkPathOps::TightBounds` deprecation warning from the C API wrapper.
- source/test builds emitted the existing `stb_image_write.h` `sprintf` deprecation warning.

## Lock Update

- updated: yes
- new `SKIA_REF`: `fb7334edc4de5833a67324e6bca1a9143dd4d607`
- new `SKIA_BASELINE_DATE`: `2025-08-01`
- reason if not updated: n/a

## Next Cycle Notes

- next cycle starts candidate selection from `fb7334edc4de5833a67324e6bca1a9143dd4d607`
- keep the next probe at the normal 1-2 week width unless path/raw or Graphite churn expands sharply
- known risks: path raw/path direction churn, Graphite capture/resource churn, ANGLE/Dawn/Vulkan rolls, and skcms dependency drift around ICC transfer-function helpers
