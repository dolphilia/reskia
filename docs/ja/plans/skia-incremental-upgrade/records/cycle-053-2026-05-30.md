# Skia Incremental Upgrade Cycle Record

作成時刻: 2026-05-30 21:26:55 JST

## Summary

- cycle: 053
- branch: `incremental-upgrade`
- baseline commit: `e56aa7634d2667c25f360c0c4073e996d54d459b`
- candidate commit: `91dc88dc70e5c7e8debbe21d4d5566d5e9f121e1`
- candidate date: `2025-05-24T01:24:35-07:00`
- candidate subject: Roll vulkan-deps from fa392b2f4964 to a0de53f5f078 (1 revision)
- result: `accepted`

## Baseline Check

- `vendor/skia-source.lock`: `SKIA_REF="e56aa7634d2667c25f360c0c4073e996d54d459b"`, `SKIA_BASELINE_DATE="2025-05-18"`
- `vendor/skia-upstream` HEAD before probe: `e56aa7634d2667c25f360c0c4073e996d54d459b`
- coverage summary before probe: `missing 0` / `deferred 0` / `partial 0` / `overcovered 0`; stale report empty
- last known build/smoke baseline: cycle 052 prebuilt/source builds, GPU smoke, and source SVG/provider/text/path smoke passed

## Candidate Selection

| item | value |
| --- | --- |
| baseline to candidate commits | 60 |
| `include` / `modules` shortstat | 21 files changed, 709 insertions(+), 509 deletions(-) |
| total source-list/dependency drift | 128 files changed, 2153 insertions(+), 1034 deletions(-) |
| primary affected areas | Android framework utils, `SkCanvas` / `SkImage` / `SkImageGenerator` / `SkSurface` / `SkRecorder` / `SkPathBuilder`, Ganesh recorder bridge, `SkUnicode`, Graphite/Vulkan/Dawn/SkSL generated ES2 deletion, `skcapture` move/removal |
| comparison candidates | local 1-week and 3-week date windows after 2025-05-24 had no additional fixed commit candidates |

The previously deferred 2-week candidate was selected because the local date windows did not provide a narrower later candidate and the regression surface was manageable. The cycle closes the deferred Vulkan/Android/Unicode/skcapture drift from cycle 052 without widening to a floating `main`.

## Public Header Delta

主要 header:

- `include/android/SkAndroidFrameworkUtils.h`
- `include/core/SkCanvas.h`
- `include/core/SkImage.h`
- `include/core/SkImageGenerator.h`
- `include/core/SkPathBuilder.h`
- `include/core/SkRecorder.h`
- `include/core/SkSurface.h`
- `include/gpu/ganesh/GrRecordingContext.h`

注目 API:

- `SkCanvas::baseRecorder`
- `SkSurface::baseRecorder`
- `GrRecordingContext::asRecorder`
- `SkImage` Graphite recorder entry points now use the common `SkRecorder*` base in public signatures
- `SkPathBuilder` header/doc and parameter signature drift

## Coverage Regression

| status | count | note |
| --- | ---: | --- |
| `missing` | 0 | final candidate and final lock matrix |
| `deferred` | 0 | final candidate and final lock matrix |
| `partial` | 0 | final candidate and final lock matrix |
| `overcovered` | 0 | final candidate and final lock matrix |
| `covered` | 2819 | final candidate and final lock matrix |
| `split_covered` | 42 | final candidate and final lock matrix |
| `false_positive` | 290 | final candidate and final lock matrix |
| `na` | 246 | final candidate and final lock matrix |
| `stale_capi` | 0 | final lock stale report empty |
| `signature_changed_review` | 0 | final lock stale report empty |

Initial candidate coverage found `missing 4`: `SkCanvas::baseRecorder`, `SkRecorder::SkRecorder`, `SkSurface::baseRecorder`, and `GrRecordingContext::asRecorder`. These are common recorder-base APIs and were classified `na` under the existing recorder ABI design bucket.

Initial candidate stale review found 16 `signature_changed_review` rows. `SkImage` recorder parameters were generalized from `skgpu::graphite::Recorder*` to `SkRecorder*`; the existing C ABI keeps graphite-recorder function names and casts the opaque graphite recorder to the common base before calling upstream. `SkPathBuilder` rows are doc/parameter-name/inline-overload drift and do not require wrapper ABI changes. The final lock stale report is empty against the updated matrix.

## Routing

| area | rows/items | decision | owner note |
| --- | ---: | --- | --- |
| `design-required` | `SkCanvas::baseRecorder`, `SkSurface::baseRecorder`, `GrRecordingContext::asRecorder`, `SkRecorder::SkRecorder` | classify `na` | Common raster/GPU recorder ABI design remains required before exposing base-recorder accessors |
| `core-codec-low-risk` | Android/core/image/surface/path-builder source and header drift | sync | No new low-risk C ABI beyond the recorder bridge fix |
| `gpu-ganesh-graphite` | Ganesh recorder bridge, Graphite/Vulkan/Dawn source drift, SkSL generated ES2 removal | sync/implement bridge | Added `SkGaneshRecorder.h`; GPU smoke covers configured Metal path |
| `text-module` | `skunicode` source drift | sync | Source-mode paragraph/shaping/unicode smoke covers configured text path |
| `removed-upstream` | Graphite ES2 generated SkSL files, `skcapture` source move/removal | remove/skip | Removed tracked generated ES2 files; `skcapture` is not part of the current Reskia mirror/build surface |

## Source/Header Sync

- synced: tracked candidate changes under `skia/DEPS`, `skia/include`, `skia/src`, and `skia/modules/skunicode/src`
- added: `skia/src/gpu/ganesh/SkGaneshRecorder.h`
- removed because upstream removed: Graphite ES2 generated SkSL files under `skia/src/sksl/generated/`
- intentionally not synced: GN/Bazel metadata
- intentionally not mirrored: `skcapture` move to `src/capture`, because the current Reskia mirror/build surface does not reference it

## C API Catch-up

- updated: `skia/capi/sk_image.cpp`
- added includes for `include/core/SkRecorder.h` and `include/gpu/graphite/Recorder.h`
- added helper `as_sk_recorder(reskia_graphite_recorder_t*)`
- updated `SkImage_makeScaledWithRecorder`, `SkImage_makeColorSpaceWithRecorder`, and `SkImage_makeColorTypeAndColorSpaceWithRecorder` to pass `SkRecorder*`
- classified `na`: common recorder-base accessors and `SkRecorder` construction; no broad recorder ABI was added in this cycle

## Verification

| gate | result | command/log |
| --- | --- | --- |
| coverage regeneration | PASS | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output docs/ja/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv ...` -> `missing 0` / `deferred 0` / `partial 0` / `overcovered 0` |
| stale C API report | PASS | final lock stale report empty |
| prebuilt configure/build | PASS | `cmake -S skia -B skia/cmake-build-codex-cycle053-prebuilt -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle053-prebuilt -j 8` |
| source configure/build | PASS | `cmake -S skia -B skia/cmake-build-codex-cycle053-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle053-source -j 8` |
| GPU smoke | PASS | `cmake -S skia -B skia/cmake-build-codex-cycle053-gpu-smoke -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_GPU_GANESH=ON -DRESKIA_ENABLE_GPU_GRAPHITE=ON -DRESKIA_ENABLE_GPU_METAL=ON`; build passed; `ctest --test-dir skia/cmake-build-codex-cycle053-gpu-smoke -R 'c_skia_gpu_(context|surface)_capi_smoke' --output-on-failure` -> 2/2 pass |
| source SVG/provider/text/path smoke | PASS | `cmake -S skia -B skia/cmake-build-codex-cycle053-source-smoke -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_SKPARAGRAPH=ON`; build passed; `ctest --test-dir skia/cmake-build-codex-cycle053-source-smoke -R 'c_skia_(shaping_smoke|shaper_capi_smoke|svg_dom_capi_smoke|skresources_capi_smoke|unicode_capi_smoke|skparagraph_smoke|paragraph_builder_capi_smoke|path_invalid_input_smoke|path_builder_invalid_input_smoke)' --output-on-failure` -> 9/9 pass |
| final lock coverage | PASS | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream --output .tmp/skia-upgrade-cycle-053/final-lock-public-api-coverage-matrix.csv ...` -> `missing 0` / `deferred 0` / `partial 0` / `overcovered 0`; stale report empty |

Notes:

- prebuilt/source/GPU builds emitted known non-fatal macOS deployment-target warnings from prebuilt static archives.
- builds emitted existing `SkPathOps::TightBounds` deprecation warnings.
- GPU build emitted a non-fatal Metal `fastMathEnabled` deprecation warning and existing test `sprintf` warning.

## Lock Update

- updated: yes
- new `SKIA_REF`: `91dc88dc70e5c7e8debbe21d4d5566d5e9f121e1`
- new `SKIA_BASELINE_DATE`: `2025-05-24`
- reason if not updated: n/a

## Next Cycle Notes

- next candidate target: compare fixed commits after `91dc88dc70e5c7e8debbe21d4d5566d5e9f121e1`
- width adjustment: local 1-week and 3-week date windows after 2025-05-24 previously had no matching refs; re-check availability before widening
- known risks: Android/source-list churn, `skcapture` move/removal, `SkImage`/`SkCanvas`/`SkRecorder` common recorder ABI, `SkUnicode` drift, Vulkan/Dawn optional backend drift, Graphite precompile ABI design
