# Skia Incremental Upgrade Cycle 017

作業日時: 2026-05-23 14:00:30 JST

## Summary

- baseline: `cd75e467271917846f2b53277028168255e4f485` (`2024-04-01`)
- accepted candidate: `8567db100d685f017915d30996905363fae2658d`
- candidate date: `2024-04-08T21:47:34Z`
- subject: `Fix minor bugs in the SkSL PrettyPrint algorithm.`
- result: accepted

## Candidate Selection

1-week date-end candidate を採用した。2-week/3-week 候補は include/modules と broad surface の差分が広がり、cycle 017 では SkBitmap/SkStream/SkShaper factory と SkSL source-list drift を中心に閉じられる 1-week 幅を選んだ。

| candidate | result | note |
| --- | --- | --- |
| `8567db100d685f017915d30996905363fae2658d` | accepted | 1-week candidate |
| 2-week candidate | deferred | public/module drift が大きいため次 cycle 以降で再評価 |
| 3-week candidate | deferred | GPU/text/SVG drift がさらに広がるため deferred |

## Coverage And Routing

Final coverage:

- `covered 2803`
- `false_positive 276`
- `na 210`
- `no_public_methods_found 106`
- `split_covered 33`
- `missing 0`
- `deferred 0`
- `partial 0`
- `overcovered 0`

Stale/signature review:

- stale C API report は `stale_capi 0`。
- `signature_changed_review` は `GrBackendTexture` constructor 3 rows のみ。
- `GrBackendTexture` の差分は upstream constructor に `std::string_view label = {}` が増えたもの。既存 C ABI は default label 相当として互換維持し、cycle 017 では wrapper signature を変更しない。

Routing:

- core/bitmap: `SkBitmap::setColorSpace` を C API 化。
- core/stream: upstream で削除された `SkMemoryStream::skipToAlign4` に対応する C API と smoke 呼び出しを削除。
- text-module: `SkShapers::Factory` を C API 化し、primitive factory と iterator/unicode 取得の smoke を追加。
- SVG internal: `SkSVGFilter::applyProperties`、`SkSVGRenderContext` constructor、`SkSVGRenderContext::makeBidiRunIterator`、`makeScriptRunIterator` は internal render-context helper として `false_positive` / `na` に分類。
- source-list: `SkShaper_factory.cpp` と `SkSLEliminateUnnecessaryBraces.cpp` を CMake source list に追加。

## Source/Header Sync

candidate から `include`、`src`、`modules/skottie`、`modules/skplaintexteditor`、`modules/skshaper`、`modules/skunicode`、`modules/svg`、`gn`、`DEPS` の対象差分を同期した。GN/DEPS は参照用として同期し、CMake build の入口は従来通り `skia/CMakeLists.txt`。

主な同期:

- `include/core/SkBitmap.h`, `SkStream.h`, `SkImageInfo.h`
- `include/gpu/GrBackendSurface.h`
- `modules/skshaper/include/SkShaper.h`
- `modules/skshaper/include/SkShaper_factory.h`
- `modules/skshaper/src/SkShaper_factory.cpp`
- `modules/svg/include/SkSVGRenderContext.h`
- `src/sksl/transform/SkSLEliminateUnnecessaryBraces.cpp`

## C API Catch-up

- Added `SkBitmap_setColorSpace`.
- Added `reskia_shapers_factory_t`.
- Added `SkShapers_Primitive_Factory`.
- Added `SkShapers_Factory_ref`, `SkShapers_Factory_unref`, `SkShapers_Factory_release`.
- Added `SkShapers_Factory_makeShaper`.
- Added `SkShapers_Factory_makeBidiRunIterator`.
- Added `SkShapers_Factory_makeScriptRunIterator`.
- Added borrowed `SkShapers_Factory_getUnicode`.
- Removed stale `SkMemoryStream_skipToAlign4`.
- Updated public API generator prefix mapping for `SkShapers::Factory`.

## Build Fixes

- Added `modules/skshaper/src/SkShaper_factory.cpp` to prebuilt and source CMake targets.
- Added `src/sksl/transform/SkSLEliminateUnnecessaryBraces.cpp` to core source list to resolve `SkSL::Transform::EliminateUnnecessaryBraces`.
- Added external ICU compatibility compile definitions for `SkBidiFactory_icu_subset.cpp` so the source build can link local ICU symbols without bundled `_skia` names.

## Verification

| gate | result | command/log |
| --- | --- | --- |
| coverage regeneration | pass | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output .tmp/skia-upgrade-cycle-017/after-overrides-public-api-coverage-matrix.csv --previous-matrix docs/ja/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --stale-output .tmp/skia-upgrade-cycle-017/after-overrides-stale-capi.csv` |
| stale C API report | pass | `stale_capi 0`; `signature_changed_review 3` for `GrBackendTexture` label default |
| prebuilt configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle017-prebuilt -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle017-prebuilt -j 8` |
| source configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle017-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle017-source -j 8` |
| GPU smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle017-gpu-smoke -R 'c_skia_gpu_(context|surface)_capi_smoke' --output-on-failure` -> 2/2 pass |
| source SVG/provider smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle017-source-smoke -R 'c_skia_(shaping_smoke|shaper_capi_smoke|svg_dom_capi_smoke|skresources_capi_smoke|unicode_capi_smoke)' --output-on-failure` -> 5/5 pass |
| Skottie/SKSG optional smoke | pass | `ctest --test-dir skia/cmake-build-codex-cycle017-skottie-sksg-smoke -R 'c_skia_(skottie_smoke|sksg_smoke|sksg_capi_smoke)' --output-on-failure` -> 3/3 pass |

Notes:

- prebuilt/GPU link では `libsvg.a` の macOS 15.1 object を macOS 15.0 target に link する既存 warning が出る。
- full GPU smoke build は既存 `test_c_skia` の old typed C API smoke compile error により失敗するため、cycle gate 対象の GPU smoke target を個別 build して検証した。
- 誤って `test_skottie_sksg_capi_smoke` target を一度指定したが、その target は存在しない。正しい target は `test_skottie_smoke`、`test_sksg_smoke`、`test_sksg_capi_smoke`。

## Lock Update

`vendor/skia-source.lock`:

- `SKIA_REF="8567db100d685f017915d30996905363fae2658d"`
- `SKIA_BASELINE_DATE="2024-04-08"`

`vendor/skia-upstream` と `vendor/skia-upstream-candidate` は accepted candidate `8567db100d685f017915d30996905363fae2658d` に同期済み。

## Next Cycle Notes

- 次サイクルでは accepted baseline `8567db100d685f017915d30996905363fae2658d` から 1-week candidate を第一候補として選ぶ。
- 2-week/3-week 候補は diff と coverage regression が管理可能な場合のみ採用する。
- `GrBackendTexture` の label default 付き constructor signature review は、次サイクルでも stale report に残る場合、C ABI に label-aware constructor を追加するか引き続き default-label 互換として記録する。
