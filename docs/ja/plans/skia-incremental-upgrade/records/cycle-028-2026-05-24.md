# Skia Incremental Upgrade Cycle 028

作業日時: 2026-05-24 10:37:32 JST

## Summary

- baseline: `7c69f39fa85b3cca07c7d433a396011e01c88f34` (`2024-07-02`)
- accepted candidate: `c73cff97952aa15e01985a35e5c6575b4eb50454`
- candidate date: `2024-07-10T21:55:27Z`
- subject: `Remove assert in SubRun deserialization`
- result: accepted

## Candidate Selection

cycle 027 の next note に従い、Bazel metadata churn と `PrecompileShader` / GL helper / private Graphite context option move を含む 1-week 候補を採用した。

| candidate | result | note |
| --- | --- | --- |
| `c73cff97952aa15e01985a35e5c6575b4eb50454` | accepted | 66 commits; `include`/`modules` drift 100 files, mostly tracked Bazel metadata plus 9 non-Bazel public/private headers |
| `84d893159af295c4dc61f408f227cf37916b5b55` | deferred | 182 commits from baseline; `include`/`modules` drift 111 files; broader Graphite/SkSL/source drift |
| `06b26a1d51d71bb71d9f50ff58bc6d315dbbba21` | deferred | 301 commits from baseline; `include`/`modules` drift 119 files; too large for this cycle |

accepted candidate の `include` / `modules` diff は `100 files changed, 115 insertions(+), 366 deletions(-)`。`DEPS` / `gn` / `bazel` / `include` / `modules` / `src` の source/dependency drift は `469 files changed, 2964 insertions(+), 1479 deletions(-)`。

主要 public/private header:

- `include/gpu/GrContextThreadSafeProxy.h`
- `include/gpu/GrDriverBugWorkarounds.h`
- `include/gpu/GrYUVABackendTextures.h`
- `include/gpu/gl/GrGLAssembleHelpers.h`
- `include/gpu/gl/GrGLAssembleInterface.h`
- `include/gpu/gl/GrGLExtensions.h`
- `include/gpu/graphite/precompile/PrecompileShader.h`
- `include/private/base/SkAlign.h`
- `include/private/gpu/graphite/ContextOptionsPriv.h` -> `src/gpu/graphite/ContextOptionsPriv.h`

## Coverage And Routing

Initial candidate coverage:

- `covered 2817`
- `false_positive 289`
- `na 222`
- `no_public_methods_found 106`
- `split_covered 33`
- `missing 3`

Initial stale C API report:

- `stale_capi 0`
- `signature_changed_review 0`

Final coverage:

- `covered 2817`
- `false_positive 289`
- `na 225`
- `no_public_methods_found 106`
- `split_covered 33`
- `missing 0`
- `deferred 0`
- `partial 0`
- `overcovered 0`

Final stale C API report:

- `stale_capi 0`
- `signature_changed_review 0`

Routing:

- `design-required`: `PrecompileShader::makeWithLocalMatrix(bool) const`, `makeWithColorFilter(sk_sp<PrecompileColorFilter>) const`, and `makeWithWorkingColorSpace(sk_sp<SkColorSpace>) const` remain routed to the existing Graphite precompile ownership/design policy.
- `gpu-ganesh-graphite`: Graphite precompile shader implementation changes and `ContextOptionsPriv` private header move were source-synced.
- `core-codec-low-risk`: JPEG utility/source and `SkStroke`/`SkTHash` source updates were source-synced.
- `text-module`: `src/text/gpu/SubRunContainer.cpp` assert removal was source-synced.
- `removed-upstream`: no public C API removal was required. `include/private/gpu/graphite/ContextOptionsPriv.h` was moved to `src/gpu/graphite/ContextOptionsPriv.h`.

## Source/Header Sync

Synced tracked candidate drift from `DEPS`, public/private `include`, mirrored `src`, and tracked `BUILD.bazel` metadata under mirrored trees.

Main sync:

- Ganesh and Graphite source updates, including GL helper updates and Graphite precompile shader wrappers.
- `ContextOptionsPriv.h` moved from `include/private/gpu/graphite/` to `src/gpu/graphite/`.
- SkSL/codegen and generated graphite vertex shader updates.
- Core stroker/hash updates, JPEG utility updates, text GPU SubRun deserialization assert removal, and CoreText cleanup.
- Tracked Bazel metadata under mirrored `include`, `modules`, and `src` trees.

CMake/source-list:

- No CMake source-list changes were required.
- The `ContextOptionsPriv.h` header move required adding `skia/src/gpu/graphite/ContextOptionsPriv.h` and removing the old private include header.

Intentionally not synced:

- Root `bazel/` metadata and untracked root `gn/*.gni` metadata remain outside Reskia's tracked mirror surface.

## C API Catch-up

Implemented:

- No new C API entry points.

Removed because upstream removed:

- No public C API removal was required.

Classified:

- Updated existing `PrecompileShader` design-required override signatures for the new `bool` and `const` forms.

## Verification

| gate | result | command/log |
| --- | --- | --- |
| initial coverage regression | pass after routing | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output .tmp/skia-upgrade-cycle-028/candidate-public-api-coverage-matrix.csv --previous-matrix docs/ja/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --stale-output .tmp/skia-upgrade-cycle-028/candidate-stale-capi.csv` |
| canonical coverage regeneration | pass | `python3 scripts/generate_public_api_coverage.py --skia-root vendor/skia-upstream-candidate --output docs/ja/plans/c-binding-remediation/checklists/public-api-coverage-matrix.csv --previous-matrix .tmp/skia-upgrade-cycle-028/previous-public-api-coverage-matrix.csv --stale-output docs/ja/plans/c-binding-remediation/checklists/public-api-stale-capi.csv` |
| stale C API report | pass | canonical stale report has 0 rows |
| prebuilt configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle028-prebuilt -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle028-prebuilt -j 8` |
| source configure/build | pass | `cmake -S skia -B skia/cmake-build-codex-cycle028-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`; `cmake --build skia/cmake-build-codex-cycle028-source -j 8` |
| GPU smoke build/ctest | pass | `cmake -S skia -B skia/cmake-build-codex-cycle028-gpu-smoke -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_GPU_GANESH=ON -DRESKIA_ENABLE_GPU_GRAPHITE=ON -DRESKIA_ENABLE_GPU_METAL=ON`; `cmake --build ...`; `ctest --test-dir skia/cmake-build-codex-cycle028-gpu-smoke -R 'c_skia_gpu_(context|surface)_capi_smoke' --output-on-failure` -> 2/2 pass |
| source SVG/provider/text smoke build/ctest | pass | `cmake -S skia -B skia/cmake-build-codex-cycle028-source-smoke -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug -DRESKIA_BUILD_TESTS=ON -DRESKIA_ENABLE_SKPARAGRAPH=ON`; `cmake --build ...`; `ctest --test-dir skia/cmake-build-codex-cycle028-source-smoke -R 'c_skia_(shaping_smoke|shaper_capi_smoke|svg_dom_capi_smoke|skresources_capi_smoke|unicode_capi_smoke|skparagraph_smoke|paragraph_builder_capi_smoke|path_invalid_input_smoke)' --output-on-failure` -> 8/8 pass |

Notes:

- Existing warnings observed: Metal `fastMathEnabled` deprecation, `stb_image_write.h` `sprintf` deprecation, and prebuilt `libsvg.a` macOS deployment-version linker warning.

## Lock Update

`vendor/skia-source.lock`:

- `SKIA_REF="c73cff97952aa15e01985a35e5c6575b4eb50454"`
- `SKIA_BASELINE_DATE="2024-07-10"`

`vendor/skia-upstream` と `vendor/skia-upstream-candidate` は accepted candidate `c73cff97952aa15e01985a35e5c6575b4eb50454` に同期済み。

## Next Cycle Notes

- 次サイクルでは accepted baseline `c73cff97952aa15e01985a35e5c6575b4eb50454` から再比較する。
- 1-week/2-week 候補を再比較し、deferred した `84d893159af295c4dc61f408f227cf37916b5b55` 周辺を第一候補にする。
- 既知リスクは Graphite/SkSL drift、Metal/Vulkan optional backend drift、CoreText/text cleanup、Bazel metadata churn。
