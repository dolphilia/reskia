# Skia Incremental Upgrade Cycle 016

作業日時: 2026-05-23 JST

## Summary

- baseline: `7ffd936a66df500b2275695f6a58208163f31518` (`2024-03-25`)
- accepted candidate: `cd75e467271917846f2b53277028168255e4f485`
- candidate date: `2024-04-01T21:33:54Z`
- subject: `[paragraph] TypefaceFontProvider impl more FontMgr`
- result: accepted

## Candidate Selection

1-week date-end candidate を採用した。2-week/3-week 候補は public/module drift と C API routing が大きく、cycle 016 では Metal backend surface namespace migration、Graphite compressed texture/readPixels 追従、Paragraph TypefaceFontProvider migration を中心に閉じられる 1-week 幅を選んだ。

| candidate | result | commits | include/modules shortstat | broad surface shortstat |
| --- | --- | ---: | --- | --- |
| `cd75e467271917846f2b53277028168255e4f485` | accepted | 108 | 31 files, +455/-274 | 136 files, +4438/-1464 |
| `8567db100d685f017915d30996905363fae2658d` | deferred | 205 | 93 files, +1402/-723 | 322 files, +8408/-3582 |
| `b159229f2174800f6655f7b7dbba01d7bd3d5d48` | deferred | 302 | 124 files, +2605/-1365 | 425 files, +12069/-5864 |

2-week candidate probe は `missing 12`、3-week candidate probe は `missing 13` になったため、次サイクル以降で再比較する。

## Coverage And Routing

Initial candidate coverage:

- `missing 3`
- stale/signature review rows: 10 (`stale_capi 5`, `signature_changed_review 5`)

Routing:

- GPU/Metal: old `GrBackendFormat_*Mtl` / `GrBackendTexture_getMtlTextureInfo` / `GrBackendRenderTarget_getMtlTextureInfo` C APIs を upstream の namespace-style helpers に合わせて rename し、stale aliases を削除。
- GPU/Graphite: `Recorder::updateCompressedBackendTexture` と `TextureInfo::compressionType` を C API 化。
- image/Graphite test utility: `SkImage::readPixelsGraphite` を C API 化。ただし upstream member は `GRAPHITE_TEST_UTILS` 限定のため、通常 build では false を返す guarded wrapper とした。
- text-module: `TypefaceFontProvider` の FontMgr 実装拡張に追従し、source smoke では CoreText FontMgr から登録用 typeface を作るようテストを更新。
- source-list: new `SkBidiFactory_icu_full.cpp` / `SkBidiFactory_icu_subset.cpp` を `skunicode` target に追加し、ローカル third_party ICU の versioned symbol rename に合わせて subset wrapper の固定 `_skia` suffix 前提を外した。
- encode: upstream header に残る `SkICCFloatXYZD50ToGrid16Lab` / `SkICCFloatToTable16` の public declarations に対し、Reskia link 互換のため定義を復元。

Final coverage:

- `covered 2798`
- `false_positive 275`
- `na 208`
- `no_public_methods_found 106`
- `split_covered 33`
- `missing 0`
- `deferred 0`
- `partial 0`
- `overcovered 0`
- stale C API rows: 0

Signature review:

- `GrBackendRenderTarget` constructor rows remain ABI-compatible through existing `GrBackendRenderTarget_new` / `newCopy` / `newMock` wrappers.
- `TypefaceFontProvider::onCreateStyleSet` and `onLegacyMakeTypeface` remain covered by existing C APIs; implementation behavior changed upstream but C ABI did not need a signature change.

## Source/Header Sync

candidate から `include`、`src`、`modules/skottie`、`modules/skparagraph`、`modules/skplaintexteditor`、`modules/skshaper`、`modules/skunicode`、`modules/svg` の対象差分 125 files を同期し、2 files を upstream 削除に追従して削除した。Bazel/GN/test/GM/CanvasKit/pathkit/jetski 系メタデータは対象外。

主な同期:

- `include/core/SkImage.h`, `SkPath.h`, `SkSize.h`
- `include/gpu/GrBackendSurface.h`
- `include/gpu/ganesh/mtl/GrMtlBackendSurface.h`
- `modules/skparagraph/include/TypefaceFontProvider.h`
- `modules/skunicode/src/SkBidiFactory_icu_full.{cpp,h}`
- `modules/skunicode/src/SkBidiFactory_icu_subset.{cpp,h}`
- Graphite/Ganesh/Metal GPU 周辺 source

削除:

- `include/gpu/mtl/GrMtlBackendContext.h`
- `include/gpu/mtl/GrMtlTypes.h`

## C API Catch-up

- Added `Graphite_Recorder_updateCompressedBackendTexture`.
- Added `Graphite_TextureInfo_compressionType`.
- Added `SkImage_readPixelsGraphite` with `GRAPHITE_TEST_UTILS` guard.
- Renamed Metal backend surface C APIs:
  - `GrBackendFormat_MakeMtl` -> `GrBackendFormats_MakeMtl`
  - `GrBackendFormat_asMtlFormat` -> `GrBackendFormats_AsMtlFormat`
  - `GrBackendTexture_getMtlTextureInfo` -> `GrBackendTextures_GetMtlTextureInfo`
  - `GrBackendRenderTarget_getMtlTextureInfo` -> `GrBackendRenderTargets_GetMtlTextureInfo`
- Removed stale `GrBackendTexture_hasMipMaps` alias; `GrBackendTexture_hasMipmaps` remains.
- Updated GPU/image/paragraph smoke tests for the new API surface.

## Verification

- coverage generator: pass
- final coverage matrix: `missing 0` / `deferred 0` / `partial 0` / `overcovered 0`
- stale C API report: `stale_capi 0`
- prebuilt configure/build:
  - `cmake -S skia -B skia/cmake-build-codex-cycle016-prebuilt -DCMAKE_BUILD_TYPE=Debug`
  - `cmake --build skia/cmake-build-codex-cycle016-prebuilt --target reskia -j 8`
  - result: pass
- source configure/build:
  - `cmake -S skia -B skia/cmake-build-codex-cycle016-source -DRESKIA_DEPS_MODE=source -DCMAKE_BUILD_TYPE=Debug`
  - `cmake --build skia/cmake-build-codex-cycle016-source --target reskia -j 8`
  - result: pass
- GPU smoke:
  - `ctest --test-dir skia/cmake-build-codex-cycle016-gpu-smoke -R 'c_skia_gpu_surface_capi_smoke|c_skia_gpu_context_capi_smoke' --output-on-failure`
  - result: 2/2 pass
- source SVG/provider/text smoke:
  - `ctest --test-dir skia/cmake-build-codex-cycle016-source-smoke -R 'c_skia_svg_dom_capi_smoke|c_skia_svg_image_capi_smoke|c_skia_svg_types_capi_smoke|c_skia_skresources_capi_smoke|c_skia_unicode_capi_smoke|c_skia_shaping_smoke|c_skia_shaper_capi_smoke|c_skia_skparagraph_smoke|c_skia_paragraph_font_collection_capi_smoke|c_skia_paragraph_typeface_font_provider_capi_smoke' --output-on-failure`
  - result: 10/10 pass
- Skottie/SKSG optional smoke:
  - `ctest --test-dir skia/cmake-build-codex-cycle016-skottie-sksg-smoke -R 'c_skia_skottie_smoke|c_skia_sksg_smoke|c_skia_sksg_capi_smoke' --output-on-failure`
  - result: 3/3 pass

Notes:

- prebuilt/GPU link では `libsvg.a` の macOS 15.1 object を macOS 15.0 target に link する既存 warning が出る。
- GPU smoke build では Metal SDK の `fastMathEnabled` deprecation warning が出る。
- full `cmake --build ... -j 8` は既存 `test_c_skia` の old typed C API smoke compile error を避けるため、cycle gate 対象 smoke target を個別 build して検証した。

## Lock Update

`vendor/skia-source.lock`:

- `SKIA_REF="cd75e467271917846f2b53277028168255e4f485"`
- `SKIA_BASELINE_DATE="2024-04-01"`

`vendor/skia-upstream` と `vendor/skia-upstream-candidate` は accepted candidate `cd75e467271917846f2b53277028168255e4f485` に同期済み。

## Next Cycle Notes

- 次サイクルでは 1-week/2-week/3-week 候補を再比較する。
- 2-week 候補 `8567db100d685f017915d30996905363fae2658d` では `missing 12` と stale/signature review 14 rows が見えているため、Graphite/Ganesh GPU drift と text-module drift を優先 routing する。
- Dawn/WebGPU optional backend rows は引き続き `RESKIA_ENABLE_GPU_DAWN` と handle policy が整うまで portable C ABI から外す。
